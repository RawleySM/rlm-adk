        # Validation Report: EH-005 — on_pf_change

        **Category**: event_handlers
        **Validated**: 2026-03-23 06:30
        **Verdict**: PASS

        ---

        ## Validation Report: EH-005

### Summary
**PASS** — The `on_pf_change` event handler is correctly defined, wired to the `ui.select` widget, and its target `set_provider_fake_fixture()` correctly updates state with bidirectional mutual exclusion.

### Evidence

1. **Handler definition** — `live_app.py:400-402`: `def on_pf_change(e)` calls `controller.set_provider_fake_fixture(str(e.value or ""))` followed by `live_ui.refresh_all()`.

2. **Widget wiring** — `live_app.py:482-488`: `ui.select(options=pf_options, value=controller.state.selected_provider_fake_fixture or None, label="Select provider-fake", with_input=False, on_change=on_pf_change)` — the dropdown is correctly bound with `on_change=on_pf_change`.

3. **Controller method** — `live_controller.py:81-85`: `set_provider_fake_fixture(self, fixture_stem: str)` sets `self.state.selected_provider_fake_fixture = fixture_stem`, clears `self.state.replay_path = ""` when `fixture_stem` is truthy (mutual exclusion), and sets `self.state.launch_error = None`.

4. **Inverse mutual exclusion** — `live_controller.py:75-79`: `set_replay_path()` clears `self.state.selected_provider_fake_fixture = ""` when `replay_path` is truthy, confirming bidirectional exclusion.

5. **Model fields** — `live_models.py:337-338`: `available_provider_fake_fixtures: list[str] = field(default_factory=list)` and `selected_provider_fake_fixture: str = ""` both exist with correct types and defaults.

6. **Downstream propagation** — `live_controller.py:96-100`: `launch_replay()` checks `self.state.selected_provider_fake_fixture` first and routes to `prepare_provider_fake_launch()`, confirming the selected fixture actually drives runtime behavior.

### Bugs Found
None.

### Validation Notes
The full data-flow chain was validated statically: dropdown widget → `on_pf_change` handler → `controller.set_provider_fake_fixture()` → state mutation with mutual exclusion → downstream consumption in `launch_replay()`. All code paths are present and logically correct. The mutual exclusion is implemented identically in both directions (pf clears replay at line 84, replay clears pf at line 78), and the `or ""` guard in the handler (line 401) ensures `None` values from the dropdown are normalized to empty strings before reaching the controller. The only caveat is that this is a static analysis — actual UI event propagation through NiceGUI's event system is not exercised, but the wiring (`on_change=on_pf_change`) follows NiceGUI's documented pattern.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer traced the complete data-flow chain from the NiceGUI `ui.select` widget's `on_change` binding through the handler function, into the controller method, and verified the downstream consumption in `launch_replay()`. The evidence is specific — citing exact line numbers and quoting actual code patterns — not just confirming that names exist. The report validates behavior (mutual exclusion logic, None-to-empty-string normalization, bidirectional clearing) rather than merely confirming structural presence. The acknowledged limitation (static analysis only, no live UI event firing) is honest and appropriate for this validation context. The line-number citations are consistent and the logic described is coherent across all six evidence points.

REWARD_HACK_DETECTED: no
