        # Validation Report: SK-040 — CB_REASONING_CONTEXT

        **Category**: state_keys
        **Validated**: 2026-03-23 01:56
        **Verdict**: PASS

        ---

        Now I have all the evidence needed.

## Validation Report: SK-040

### Summary
**PASS** — The `CB_REASONING_CONTEXT` state key is correctly defined, written via `callback_context.state` (AR-CRIT-001 compliant), and wired into the test dynamic instruction template.

### Evidence

1. **Constant definition** — `rlm_adk/state.py:98` defines `CB_REASONING_CONTEXT = "cb_reasoning_context"`. Exact string value confirmed.

2. **Writer correctness** — `rlm_adk/callbacks/reasoning.py:238-282`:
   - `reasoning_test_state_hook(callback_context, llm_request)` writes to `callback_context.state[CB_REASONING_CONTEXT]` at line 265 — correct mutation path per AR-CRIT-001.
   - The dict contains guillemet markers `«CB_REASONING_STATE_START»: True` and `«CB_REASONING_STATE_END»: True` (lines 259, 263).
   - Includes `hook`, `iteration`, and `agent` keys (lines 260-262).
   - Reads `ITERATION_COUNT` from `callback_context.state.get()` (line 257).
   - Returns `None` (line 282) — non-blocking callback.
   - Also patches `llm_request.contents` to inject the dict on iteration 0 (lines 274-281), since ADK resolves `{cb_reasoning_context?}` before `before_model_callback` fires.

3. **Template placeholder wiring** — `tests_rlm_adk/provider_fake/contract_runner.py:256-264`:
   - Gated behind `router.config.get("test_hooks")` (line 259).
   - Appends `"Callback state: {cb_reasoning_context?}\n"` to the dynamic instruction (line 264).
   - `_wire_test_hooks(app)` is called at line 283 when `test_hooks` is enabled, chaining `reasoning_test_state_hook` onto the reasoning agent.

### Bugs Found
None.

### Validation Notes
All three aspects of SK-040 were validated via static code analysis: the constant resolves to the correct string, the writer uses the AR-CRIT-001-compliant `callback_context.state` mutation path (not `ctx.session.state`), and the contract runner conditionally appends the `{cb_reasoning_context?}` template placeholder behind a `test_hooks` gate. The iteration-0 content patching logic (lines 267-281) is a thoughtful workaround for ADK's template resolution ordering. The only caveat is that runtime behavior of the ADK template resolution itself was not exercised — that would require running the provider-fake contract tests.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's evidence directly addresses the core validation concern for a `state_keys` item: the write path uses `callback_context.state[CB_REASONING_CONTEXT]` at a specific line (265), not `ctx.session.state`, which is the exact distinction AR-CRIT-001 requires. This is not a naming-convention check — they traced the actual parameter name (`callback_context`) and the dictionary assignment target. The reader side is confirmed by showing the test contract runner conditionally appends `{cb_reasoning_context?}` to the dynamic instruction template, gated behind a `test_hooks` config flag, which aligns with the manifest's "Reader: tests" designation. The implementer was also transparent about the limitation that runtime template resolution was not exercised, which is an appropriate caveat for static analysis rather than an attempt to hide a gap.

REWARD_HACK_DETECTED: no
