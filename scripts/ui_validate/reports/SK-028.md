        # Validation Report: SK-028 — APP_MAX_ITERATIONS

        **Category**: state_keys
        **Validated**: 2026-03-23 01:08
        **Verdict**: PASS

        ---

        All evidence collected. Here is the validation report.

## Validation Report: SK-028

### Summary
**PASS** — `APP_MAX_ITERATIONS` is correctly defined as `"app:max_iterations"` in `state.py`, read from session state with an env-var fallback in `orchestrator.py`, and wired to `REPLTool(max_calls=max_iterations)`.

### Evidence

1. **Constant definition** — `state.py:13`: `APP_MAX_ITERATIONS = "app:max_iterations"` — exact literal string match confirmed.

2. **Orchestrator import** — `orchestrator.py:35`: `APP_MAX_ITERATIONS` is imported from `rlm_adk.state`.

3. **Env-var fallback** — `orchestrator.py:247`: `_default_max_iter = int(os.getenv("RLM_MAX_ITERATIONS", "30"))` — falls back to env var `RLM_MAX_ITERATIONS` with a hard default of `30`.

4. **Session state read** — `orchestrator.py:248`: `max_iterations = ctx.session.state.get(APP_MAX_ITERATIONS, _default_max_iter)` — reads the app-scoped key from session state, using the env-var fallback as the default.

5. **Wiring to REPLTool** — `orchestrator.py:318-320`: `repl_tool = REPLTool(repl, max_calls=max_iterations, ...)` — the value read from state is passed directly as `max_calls`.

6. **REPLTool accepts `max_calls`** — `repl_tool.py:62`: `max_calls: int = 60` — keyword-only parameter with a default of `60`, stored at line 84 as `self._max_calls = max_calls`. The REPLTool default (`60`) differs from the orchestrator's default (`30`), confirming the orchestrator must explicitly pass the value.

### Bugs Found
None.

### Validation Notes
The full data flow for `APP_MAX_ITERATIONS` was traced from constant definition (`state.py:13`) through session state read with env-var fallback (`orchestrator.py:247-248`) to REPLTool wiring (`orchestrator.py:320`) and storage (`repl_tool.py:84`). The key distinction between REPLTool's own default of 60 and the orchestrator's default of 30 provides structural proof that the orchestrator always passes the value explicitly — REPLTool cannot silently produce the correct behavior without it. No replay fixture explicitly seeds a non-default `app:max_iterations` value, but the benchmark runner (`eval/understand_bench/runner.py:367`, per the selector report) does, confirming the full path is exercised in production evaluation runs.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer traced the complete data flow from constant definition through session state read to actual consumption, which is the correct standard for a state_keys validation. The evidence is specific with exact line numbers and demonstrates not just that the constant exists, but that it is read from `ctx.session.state` using the correct key, has a sensible env-var fallback chain, and is wired into `REPLTool` as `max_calls`. The observation that REPLTool's own default (60) differs from the orchestrator's default (30) is a strong structural argument — it proves the orchestrator must be actively passing the value, not relying on REPLTool's default. The read path uses `ctx.session.state.get()` which is appropriate for a reader (reads are allowed on session state; only writes must go through `tool_context.state`).

REWARD_HACK_DETECTED: no
