        # Validation Report: SK-030 — OBS_REWRITE_TOTAL_MS

        **Category**: state_keys
        **Validated**: 2026-03-23 01:16
        **Verdict**: PASS

        ---

        Good — confirmed: all four `OBS_REWRITE_*` constants are defined only in `state.py` and never imported or used anywhere else. Now I have all the evidence needed.

## Validation Report: SK-030

### Summary
**PASS** — `OBS_REWRITE_TOTAL_MS` is correctly defined as a constant but has no state-write path. The value accumulates only as an instance attribute on `REPLTool`. Declared readers (`observability.py`, `sqlite_tracing.py`) do not reference the key at all. The dashboard's `live_loader.py` lists it in `_KNOWN_OBS_KEYS` (event-driven, no crash if absent).

### Evidence

1. **Constant definition** — `OBS_REWRITE_TOTAL_MS = "obs:rewrite_total_ms"` at `rlm_adk/state.py:66`, under the `# AST Rewrite Instrumentation` section alongside three sibling constants (`OBS_REWRITE_COUNT`, `OBS_REWRITE_FAILURE_COUNT`, `OBS_REWRITE_FAILURE_CATEGORIES`).

2. **No state write path** — Grepping for `OBS_REWRITE_TOTAL_MS` across all `.py` files in `rlm_adk/` returns only `state.py:66` (the definition). The constant is never imported by any module. There is zero occurrence of `tool_context.state[...rewrite...]`, `state[...rewrite...]`, `state_delta`, or `EventActions` referencing this key anywhere in `rlm_adk/`.

3. **Instance attribute accumulator exists** — `self._rewrite_total_ms = 0.0` initialized at `rlm_adk/tools/repl_tool.py:93`, accumulated via `self._rewrite_total_ms += _rewrite_ms` at `repl_tool.py:230`. This value is never flushed to `tool_context.state`. The accumulator lives and dies with the `REPLTool` instance.

4. **Declared readers do not reference the key** — `rlm_adk/plugins/observability.py` has zero matches for `rewrite_total_ms`. `rlm_adk/plugins/sqlite_tracing.py` has zero matches for `rewrite_total_ms`. Neither file imports `OBS_REWRITE_TOTAL_MS`.

5. **Dashboard live_loader lists the key** — `rlm_adk/dashboard/live_loader.py:66` includes `"obs:rewrite_total_ms"` in the `_KNOWN_OBS_KEYS` list. This list is consumed event-driven (state delta events); if no event ever carries this key, it simply never appears — no `KeyError` or crash.

6. **Sibling constants equally dead** — All four `OBS_REWRITE_*` constants (`COUNT`, `TOTAL_MS`, `FAILURE_COUNT`, `FAILURE_CATEGORIES`) are defined only in `state.py` and never imported or referenced in any other Python module. This is a consistent "dead key" family.

### Bugs Found

- **Bug:** `OBS_REWRITE_TOTAL_MS` (and its three sibling constants) are defined in `state.py` but never written to session state. The instance attribute `self._rewrite_total_ms` accumulates timing data that is silently discarded when the `REPLTool` instance is garbage collected.
  - **Location:** `rlm_adk/state.py:65-68` (dead constants), `rlm_adk/tools/repl_tool.py:93,230` (accumulator with no flush)
  - **Severity:** warning
  - **Fix:** Either (a) add a state-write call in `REPLTool.run_async`'s finalization path (e.g., `tool_context.state[OBS_REWRITE_TOTAL_MS] = self._rewrite_total_ms`) so the data becomes observable, or (b) remove the four dead `OBS_REWRITE_*` constants from `state.py` and the `_KNOWN_OBS_KEYS` entry in `live_loader.py` to reduce dead code. The manifest already documents this as "Writer: NONE", so this is a known gap rather than a regression.

### Validation Notes

All four test assertions from the test plan pass statically: the constant is defined with the correct value, no state-write path exists anywhere in `rlm_adk/`, the declared readers (`observability.py`, `sqlite_tracing.py`) have zero references, and `live_loader.py` lists the key in its known-keys set. The "dead key" status is confirmed and consistent with the manifest's `Writer: NONE` declaration. The main caveat is that this validation is static — a dynamic test would also confirm that no runtime code path writes the key via indirect means (e.g., a dictionary comprehension), but the complete absence of the constant's import from `state.py` into any other module makes such an indirect path impossible.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer went well beyond surface-level checks. Rather than merely confirming the constant exists in `state.py`, they traced the full lifecycle: the constant definition, the absence of any import into other modules, the instance-attribute accumulator in `repl_tool.py` that never flushes to `tool_context.state`, and the zero references in both declared readers (`observability.py`, `sqlite_tracing.py`). The grep-based evidence is direct — not circumstantial — and the conclusion that this is a "dead key family" is supported by checking all four sibling constants. The bug was correctly identified (accumulator data silently discarded) and severity of "warning" is appropriate given the manifest already declares `Writer: NONE`, making this a known design gap rather than a regression. The one minor gap — the manifest claims readers exist but they don't actually reference the key — was documented in the evidence rather than buried.

REWARD_HACK_DETECTED: no
