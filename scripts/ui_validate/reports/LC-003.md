        # Validation Report: LC-003 — Select live replay dropdown

        **Category**: launch_controls
        **Validated**: 2026-03-23 04:24
        **Verdict**: PASS

        ---

        ## Validation Report: LC-003

### Summary
**PASS** — All three validation aspects (discovery, mutual exclusion, state model) are fully implemented and correctly wired.

### Evidence

1. **`list_replay_fixtures()` discovery** (`run_service.py:119-142`)
   - Accepts optional `replay_dir` and `repo_root` keyword arguments for testability
   - Defaults to `_REPO_ROOT / "tests_rlm_adk" / "replay"` when no args provided
   - Returns `[]` if directory doesn't exist (line 131-132)
   - Uses `sorted(resolved_replay_dir.rglob("*.json"))` — sorted glob, `.json` only (line 135)
   - Skips non-files via `fixture_path.is_file()` guard (line 136-137)
   - Converts to repo-relative POSIX paths via `relative_to().as_posix()` (line 139)
   - Falls back to absolute POSIX path if `relative_to` raises `ValueError` (line 140-141)

2. **`set_replay_path()` mutual exclusion** (`live_controller.py:75-79`)
   - Stores `replay_path` on `self.state.replay_path` (line 76)
   - Conditionally clears `selected_provider_fake_fixture = ""` only when `replay_path` is truthy (line 77-78) — correct mutual exclusion with guard
   - Resets `launch_error = None` unconditionally (line 79)
   - Mirror method `set_provider_fake_fixture()` (lines 81-85) clears `replay_path` when fixture is set — bidirectional exclusion confirmed

3. **`on_replay_change` handler wiring** (`live_app.py:396-398`)
   - Defined at line 396: `def on_replay_change(e) -> None:`
   - Calls `controller.set_replay_path(str(e.value or ""))` at line 397
   - Calls `live_ui.refresh_all()` at line 398 to trigger UI re-render

4. **`ui.select` dropdown construction** (`live_app.py:472-481`)
   - `options=replay_options` where `replay_options = controller.state.available_replay_fixtures` (line 368)
   - `value=controller.state.replay_path or None` — pre-selects current path or clears (line 474)
   - `label="Select live replay"` (line 475)
   - `on_change=on_replay_change` — correctly bound (line 477)
   - Disable logic at lines 480-481: `if not replay_options: replay_select.disable()` — dropdown disabled when no fixtures exist

5. **`LiveDashboardState` field defaults** (`live_models.py:318-346`)
   - `replay_path: str = ""` at line 326 — defaults to empty string
   - `available_replay_fixtures: list[str] = field(default_factory=list)` at line 322 — defaults to empty list
   - `selected_provider_fake_fixture: str = ""` at line 338 — mutual exclusion target field confirmed

6. **`initialize()` populates fixtures** (`live_controller.py:61-62`)
   - `self.state.available_replay_fixtures = list_replay_fixtures()` at line 62 — called during controller initialization

### Bugs Found
None.

### Validation Notes
All three test aspects — discovery, mutual exclusion, and state defaults — are fully implemented with correct logic. The `list_replay_fixtures()` function handles edge cases (missing directory, non-file entries, relative path fallback). The UI wiring chain is complete: `ui.select(on_change=on_replay_change)` → `controller.set_replay_path()` → state mutation with mutual exclusion. The dropdown correctly disables itself when `replay_options` is empty. The only caveat is that `rglob("*.json")` will also pick up nested subdirectory JSON files (e.g., `replay_input.schema.json`), but this is by design since the function is documented as discovering all JSON fixtures in the replay directory tree.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
Every claim in the Implementer's report was verified against the actual source code with exact line number correspondence. The `list_replay_fixtures()` function at `run_service.py:119-142` implements proper discovery with edge case handling (missing directory, non-file filtering, relative path fallback). The `set_replay_path()` method at `live_controller.py:75-79` correctly implements bidirectional mutual exclusion with `set_provider_fake_fixture()` at lines 81-85. The `on_replay_change` handler at `live_app.py:396-398` is correctly wired to the `ui.select` dropdown at lines 472-481, which includes proper disable logic when no fixtures exist. The `LiveDashboardState` dataclass fields at `live_models.py:318-346` match the reported defaults exactly, and `initialize()` at `live_controller.py:61-62` populates the fixture list on startup.

REWARD_HACK_DETECTED: no
