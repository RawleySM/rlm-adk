        # Validation Report: SK-029 — OBS_REWRITE_COUNT

        **Category**: state_keys
        **Validated**: 2026-03-23 01:12
        **Verdict**: PASS

        ---

        ## Validation Report: SK-029

### Summary
**FAIL — Unimplemented observability gap.** The state key `obs:rewrite_count` is defined in `state.py` and the dashboard reader is wired to ingest it, but no code path ever writes the value to session state. The instance attribute `self._rewrite_count` on `REPLTool` increments correctly but is never flushed via `tool_context.state`. The manifest's claimed readers (`observability.py`, `sqlite_tracing.py`) contain zero references to this key.

### Evidence

| # | Evidence | File | Line(s) |
|---|----------|------|---------|
| 1 | `OBS_REWRITE_COUNT = "obs:rewrite_count"` — constant defined | `rlm_adk/state.py` | 65 |
| 2 | `OBS_REWRITE_COUNT` is **not** in the import block from `rlm_adk.state` | `rlm_adk/tools/repl_tool.py` | 30–45 |
| 3 | `self._rewrite_count = 0` — instance attr initialized | `rlm_adk/tools/repl_tool.py` | 92 |
| 4 | `self._rewrite_count += 1` — incremented on successful rewrite | `rlm_adk/tools/repl_tool.py` | 229 |
| 5 | No `tool_context.state[OBS_REWRITE_COUNT]` or `tool_context.state["obs:rewrite_count"]` anywhere in `repl_tool.py` | `rlm_adk/tools/repl_tool.py` | full file grep |
| 6 | `"obs:rewrite_count"` listed in `_KNOWN_OBS_KEYS` — reader-side wired | `rlm_adk/dashboard/live_loader.py` | 65 |
| 7 | Zero matches for `rewrite_count` or `OBS_REWRITE` in `observability.py` | `rlm_adk/plugins/observability.py` | — |
| 8 | Zero matches for `rewrite_count` or `OBS_REWRITE` in `sqlite_tracing.py` | `rlm_adk/plugins/sqlite_tracing.py` | — |
| 9 | Codebase-wide grep of `rlm_adk/` for `rewrite_count` returns only: `state.py` (definition), `live_loader.py` (reader), `repl_tool.py` (instance attr) — **no state write** | `rlm_adk/` | — |

### Bugs Found

- **Bug:** `OBS_REWRITE_COUNT` is never written to session state — the bridge between `self._rewrite_count` (instance attribute on `REPLTool`) and `tool_context.state[OBS_REWRITE_COUNT]` was never implemented (or was removed during the depth-scoping cleanup).
  - **Location:** `rlm_adk/tools/repl_tool.py`, missing flush after line 229 (or in a post-execution finalization block)
  - **Severity:** warning
  - **Fix:** After the REPL execution block (or in the telemetry finalizer), add:
    ```python
    from rlm_adk.state import OBS_REWRITE_COUNT, OBS_REWRITE_TOTAL_MS, OBS_REWRITE_FAILURE_COUNT
    # In the appropriate post-execution path:
    tool_context.state[OBS_REWRITE_COUNT] = self._rewrite_count
    tool_context.state[OBS_REWRITE_TOTAL_MS] = self._rewrite_total_ms
    tool_context.state[OBS_REWRITE_FAILURE_COUNT] = self._rewrite_failure_count
    ```

- **Bug:** Manifest lists `observability.py` and `sqlite_tracing.py` as readers of `obs:rewrite_count`, but neither file contains any reference to this key.
  - **Location:** Manifest entry for SK-029, `Reader` field
  - **Severity:** cosmetic (manifest metadata is stale/incorrect)
  - **Fix:** Update the manifest reader field to list only `live_loader.py` (the actual reader via `_KNOWN_OBS_KEYS`).

### Validation Notes

All six test plan checks were confirmed via direct file reads and codebase-wide grep. The constant `OBS_REWRITE_COUNT` is properly defined in `state.py:65`, the instance-level counter `self._rewrite_count` correctly increments on each AST rewrite in `repl_tool.py:229`, and the dashboard's `_KNOWN_OBS_KEYS` in `live_loader.py:65` is ready to ingest the value. However, the critical missing piece is the state flush — `OBS_REWRITE_COUNT` is never imported in `repl_tool.py` (confirmed by inspecting lines 30–45) and no `tool_context.state` write for this key exists anywhere in the codebase. This is a classic half-wired observability gap: both the producer (instance counter) and the consumer (dashboard loader) exist, but the bridge between them was never connected. The companion keys `OBS_REWRITE_TOTAL_MS` and `OBS_REWRITE_FAILURE_COUNT` (state.py:66–67) suffer the same gap.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's report is thorough and well-evidenced. It correctly identifies that `OBS_REWRITE_COUNT` is defined as a constant but never written to session state via `tool_context.state`, which matches the manifest's own declaration ("Writer: NONE (instance attr only — state write not implemented)"). The report goes beyond surface-level checks: it verified the absence of the state write across the entire `rlm_adk/` codebase via grep, confirmed the instance attribute exists and increments but is never flushed, and correctly identified that the manifest's claimed readers (`observability.py`, `sqlite_tracing.py`) contain zero references to this key while the actual reader is `live_loader.py`. The FAIL verdict is appropriate — this is a genuinely unimplemented state write, not a cosmetic issue. The severity rating of "warning" for the missing state flush is reasonable given that the manifest itself documents this as unimplemented, though one could argue it should be "error" since it means the dashboard will always show a stale/missing value.

REWARD_HACK_DETECTED: no
