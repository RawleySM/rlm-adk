        # Validation Report: SK-056 — STEP_MODE_ADVANCE_COUNT

        **Category**: state_keys
        **Validated**: 2026-03-23 03:05
        **Verdict**: PASS

        ---

        All evidence gathered. Here is the validation report:

## Validation Report: SK-056

### Summary
**FAIL — DEAD_KEY.** The constant `STEP_MODE_ADVANCE_COUNT = "step:advance_count"` is defined in `state.py` but is never written to session state by any production code, and is never read by the dashboard. Both the manifest's "Writer: orchestrator.py" and "Reader: dashboard" claims are incorrect.

### Evidence

1. **Constant exists with correct value** — `rlm_adk/state.py:125` defines `STEP_MODE_ADVANCE_COUNT = "step:advance_count"  # int — number of advances taken`. Value matches manifest.

2. **No writer in production code** — Grep of the entire `rlm_adk/` tree for both `step:advance_count` and `STEP_MODE_ADVANCE_COUNT` returns exactly one hit: the definition line in `state.py:125`. Zero references in any other production file.

3. **Claimed writer `orchestrator.py` has no reference** — Grep of `rlm_adk/orchestrator.py` for `advance_count` and `STEP_MODE` returns zero matches. The manifest's writer claim is false.

4. **Expected writer `StepModePlugin` (`plugins/step_mode.py`) has no reference** — The full 43-line source of `StepModePlugin` contains no import of, reference to, or write of `STEP_MODE_ADVANCE_COUNT` or `advance_count`. The plugin calls `step_gate.wait_for_advance()` but never increments any counter in `callback_context.state`.

5. **`step_gate.py` has no reference** — The `StepGate.advance()` method sets an asyncio event but does not write to any ADK state key. Zero matches for `advance_count` in `step_gate.py`.

6. **Dashboard has no reader** — Grep of `rlm_adk/dashboard/` for both `step:advance_count` and `STEP_MODE_ADVANCE_COUNT` returns zero matches. The manifest's "Reader: dashboard" claim is false.

7. **Prior devil's-advocate reviews independently confirm** — Multiple review reports in `ai_docs/devils-advocate-workspace/` (iterations 1 and 2, both with-skill and without-skill variants) explicitly flag this as a dead key with no writer: *"neither the existing `StepModePlugin` nor the `StepGate` writes this value to session state"* (`ai_docs/devils-advocate-workspace/iteration-1/plan-file-review/without_skill/outputs/report.md:180`).

8. **Existing test only checks constant value, not runtime write** — `tests_rlm_adk/test_step_mode_state_keys.py:31` asserts `STEP_MODE_ADVANCE_COUNT == "step:advance_count"` but does not test that any code actually writes the key to session state.

### Bugs Found

- **Bug:** `STEP_MODE_ADVANCE_COUNT` is defined in `state.py` but never written to session state by any production code path. The manifest claims "Writer: orchestrator.py" — this is false. The natural writer (`StepModePlugin.before_model_callback`) calls `step_gate.wait_for_advance()` but does not increment a counter via `callback_context.state`.
- **Location:** `rlm_adk/state.py:125` (definition), `rlm_adk/plugins/step_mode.py:41` (missing write)
- **Severity:** warning (dead key — not causing runtime errors, but the manifest is misleading and the key serves no purpose)
- **Fix:** Either (a) remove the constant from `state.py` if it's purely aspirational, or (b) add `callback_context.state[STEP_MODE_ADVANCE_COUNT] = callback_context.state.get(STEP_MODE_ADVANCE_COUNT, 0) + 1` in `StepModePlugin.before_model_callback` after `await step_gate.wait_for_advance()` returns, and add a corresponding dashboard reader. The devil's-advocate reviews recommend the same.

### Validation Notes
This validation comprehensively confirmed the dead-key status through five independent evidence paths: (1) full-tree grep of production code, (2) targeted inspection of the claimed writer (`orchestrator.py`), (3) source review of the natural writer (`step_mode.py`), (4) grep of the claimed reader (`dashboard/`), and (5) corroboration from prior devil's-advocate reviews. The only non-definition references are in tests (constant-value assertion only), documentation, prompts, and worktree copies — none of which constitute runtime writes. Static analysis is sufficient to confirm this is a dead key with high confidence, since the constant name and string literal were both searched and no indirect aliasing patterns were found.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's report is thorough and well-evidenced. It correctly identifies SK-056 as a dead key through five independent verification paths: full-tree grep for both the constant name and string literal, targeted inspection of the claimed writer (orchestrator.py), source review of the natural writer (step_mode.py and step_gate.py), grep of the claimed reader (dashboard/), and corroboration from prior independent reviews. The FAIL verdict with "DEAD_KEY" classification is the correct call — the key is defined but never written or read in production code. The severity rating of "warning" is appropriate since a dead key causes no runtime harm but indicates manifest inaccuracy.

REWARD_HACK_DETECTED: no
