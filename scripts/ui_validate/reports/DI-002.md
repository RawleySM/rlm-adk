        # Validation Report: DI-002 — repo_url template var

        **Category**: dynamic_instructions
        **Validated**: 2026-03-23 03:27
        **Verdict**: PASS

        ---

        ## Validation Report: DI-002

### Summary
**PASS** — The full `repo_url` data path is correctly wired: constant definition, session state write, and ADK template placeholder are all consistent and present.

### Evidence

1. **`DYN_REPO_URL = "repo_url"`** defined at `rlm_adk/state.py:32`. The `REPO_URL = "repo_url"` context metadata key is at line 25. Both have the value `"repo_url"`.

2. **`{repo_url?}` placeholder** exists on `rlm_adk/utils/prompts.py:110` as the first line of `RLM_DYNAMIC_INSTRUCTION`:
   ```
   Repository URL: {repo_url?}
   ```
   The `?` suffix makes it optional (ADK resolves to empty string when unset).

3. **Orchestrator writes both keys** in `rlm_adk/orchestrator.py:399-401`:
   ```python
   if self.repo_url:
       initial_state[REPO_URL] = self.repo_url
       initial_state[DYN_REPO_URL] = self.repo_url
   ```
   `REPO_URL` is the context metadata key (for callbacks/observability); `DYN_REPO_URL` is the template injection key (for ADK's `{repo_url?}` resolution). Both are set to `self.repo_url`.

4. **Constant-to-placeholder consistency**: `DYN_REPO_URL` has value `"repo_url"`, and the template placeholder is `{repo_url?}`. The key name matches exactly — ADK's state variable injection will resolve the placeholder from session state using this key.

5. **Comment documentation** at `state.py:29-31` explicitly states these keys "match the `{var?}` placeholders in `RLM_DYNAMIC_INSTRUCTION`", confirming intentional coupling.

### Bugs Found
None.

### Validation Notes
All four links in the DI-002 data chain were verified: constant definition (`state.py`), state write (`orchestrator.py`), and template placeholder (`prompts.py`). The `DYN_REPO_URL` value `"repo_url"` matches the `{repo_url?}` placeholder name character-for-character, so ADK's runtime state variable injection will correctly substitute the URL into the dynamic instruction. The only caveat is that this is static analysis — runtime resolution depends on ADK's template engine behaving as documented (replacing `{key?}` with `session.state[key]`), which is a framework guarantee not directly testable here.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer traced the complete data path from constant definition through state write to template placeholder resolution. The evidence is concrete and specific — exact file paths, line numbers, and code snippets are cited for each link in the chain. Critically, the report goes beyond surface-level checks: it verifies that the string value of `DYN_REPO_URL` (`"repo_url"`) character-matches the template placeholder `{repo_url?}`, that the orchestrator writes to both the metadata and template keys, and that the write uses `initial_state[]` (which is appropriate for session state initialization). The caveat about ADK's runtime template engine is honest and correctly scoped — the framework guarantee is reasonable to trust.

REWARD_HACK_DETECTED: no
