        # Validation Report: UC-008 — Active children metric chip

        **Category**: ui_controls
        **Validated**: 2026-03-23 04:07
        **Verdict**: PASS

        ---

        ## Validation Report: UC-008

### Summary
**PASS** — The active children metric chip is fully implemented across all three layers: data model, computation logic, and render wiring.

### Evidence

1. **Data model field `LiveRunStats.active_children`** — `live_models.py:269`: `active_children: int = 0` on the frozen dataclass `LiveRunStats`, with default 0.

2. **Data model field `LiveRunState.active_children`** — `live_models.py:295`: `active_children: int` field on `LiveRunState`.

3. **Computation logic** — `live_loader.py:820-828`: `_build_snapshot()` computes `active_children` as `len([child for child in child_summaries.get((active_depth) + 1, []) if child.status == "running"])`. This correctly counts only children at `active_depth + 1` whose status is `"running"`.

4. **Controller wiring** — `live_controller.py:691`: `active_children=self.state.stats.active_children` — the controller's `_build_run_state()` passes the stats value through to `LiveRunState`.

5. **Render wiring** — `live_app.py:223-226`: `active_children = run_state.active_children if run_state else 0` followed by `_metric_chip("active children", str(active_children))`. Falls back to `0` when `run_state` is `None`.

6. **`_metric_chip` function** — `live_app.py:548-555`: `def _metric_chip(label: str, value: str) -> None` renders a styled div with two `ui.label` elements (label + value), using pill-style border-radius and dashboard CSS variables.

### Bugs Found
None.

### Validation Notes
The full pipeline was validated statically across four files: the `LiveRunStats` and `LiveRunState` dataclasses define the `active_children` field with correct types and defaults; `_build_snapshot()` in the loader computes the count by filtering child summaries with `status == "running"` at `active_depth + 1`; the controller propagates the stat from snapshot to run state; and `live_app.py` reads the value with a safe fallback to 0 and renders it via `_metric_chip("active children", ...)`. The `_metric_chip` helper renders a `ui.label`-based styled chip consistent with the manifest's UI element specification. No gaps or missing implementations were found. The only caveat is that end-to-end rendering (actual DOM output) cannot be confirmed statically — the NiceGUI `ui.label` calls are trusted to produce visible elements.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence chain is substantive and traces the full data path from computation to rendering. The Implementer didn't just confirm field existence — they cited the actual filtering logic (`status == "running"` at `active_depth + 1`), the controller pass-through, and the specific `_metric_chip("active children", ...)` render call with its safe fallback to 0. The `_metric_chip` helper's use of `ui.label` elements matches the manifest's UI element specification. The Implementer appropriately caveatted that actual DOM output cannot be confirmed statically, which is an honest limitation rather than a gap. All six evidence points reference specific line numbers and contain the actual code logic, not just structural assertions.

REWARD_HACK_DETECTED: no
