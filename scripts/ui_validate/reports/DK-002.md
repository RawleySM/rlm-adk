        # Validation Report: DK-002 — MIGRATION_STATUS

        **Category**: dead_key_candidates
        **Validated**: 2026-03-23 03:10
        **Verdict**: PASS

        ---

        ## Validation Report: DK-002

### Summary
**PASS** — `MIGRATION_STATUS` is confirmed dead code. The constant is declared in `state.py` but never imported or referenced by any production module; the migration plugin intentionally uses instance attributes instead, per AR-CRIT-001 compliance.

### Evidence

| # | Check | Result | File | Detail |
|---|-------|--------|------|--------|
| 1 | Constant declaration | Found | `rlm_adk/state.py:117` | `MIGRATION_STATUS = "migration:status"` — the sole declaration |
| 2 | Production imports of `MIGRATION_STATUS` | **Zero** | `rlm_adk/**/*.py` | Grep for `MIGRATION_STATUS` across all `.py` files under `rlm_adk/` returned only `rlm_adk/state.py:117`. No other production module imports or references the constant. |
| 3 | String literal `"migration:status"` in production code | **Zero** | `rlm_adk/**/*.py` | Grep for `migration:status` across `rlm_adk/` returned only `rlm_adk/state.py:117`. No production code writes or reads this string literal from session state. |
| 4 | AR-CRIT-001 comment in migration.py | Found | `rlm_adk/plugins/migration.py:82-84` | Comment: *"AR-CRIT-001: after_run_callback has no delta-tracked channel (no callback_context), so migration status is stored on the plugin instance for programmatic access / logging only."* |
| 5 | Instance attribute `self.last_status` initialization | Found | `rlm_adk/plugins/migration.py:85` | `self.last_status: str \| None = None` |
| 6 | Instance attribute write on success | Found | `rlm_adk/plugins/migration.py:239` | `self.last_status = "completed"` (with AR-CRIT-001 comment at line 236-237) |
| 7 | Instance attribute write on failure | Found | `rlm_adk/plugins/migration.py:266` | `self.last_status = "failed"` (with AR-CRIT-001 comment at line 265) |
| 8 | References in non-production files | Present but irrelevant | `rlm_adk_docs/dispatch_and_state.md:303`, `ai_docs/database_spec.md`, `issues/fixed/`, stale docs | These are documentation, specs, and issue-tracking files — not runtime code paths. `database_spec.md` contains an older design that wrote to `state[MIGRATION_STATUS]`, which was superseded by the instance-attribute pattern. |

### Bugs Found

No bugs found. The constant is dead code as declared by the manifest. However:

- **Recommendation** (not a bug): `MIGRATION_STATUS`, `MIGRATION_TIMESTAMP`, and `MIGRATION_ERROR` (lines 117-119 of `state.py`) could be removed to reduce dead code surface area. The documentation references in `dispatch_and_state.md:303` and `database_spec.md` should be updated accordingly if the constants are removed.
- **Severity**: cosmetic
- **Location**: `rlm_adk/state.py:116-119`

### Validation Notes

This validation confirmed the dead-key claim through two independent search strategies: (1) searching for the Python constant name `MIGRATION_STATUS` across all production `.py` files, and (2) searching for the raw string literal `"migration:status"` to catch any direct string usage that bypasses the constant. Both searches returned zero hits outside the declaration site. The migration plugin's source code provides positive evidence of the *intentional* bypass — three explicit AR-CRIT-001 comments explain why instance attributes (`self.last_status`, `self.last_error`, `self.last_timestamp`) are used instead of session state keys. The `database_spec.md` references show this was an earlier design that was deliberately refactored away. Static grep-based validation is highly reliable for this category of check since Python import/string-usage is straightforward to search.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence is convincing because the implementer used two independent, complementary search strategies — searching for the Python identifier `MIGRATION_STATUS` and the raw string literal `"migration:status"` — both returning zero hits outside the declaration site. For a dead-key claim, grep-based absence evidence is the gold standard in Python (no dynamic metaprogramming concerns here). Crucially, the report goes beyond mere absence: it surfaces positive causal evidence (AR-CRIT-001 comments at three locations in `migration.py`) explaining *why* the key was intentionally abandoned in favor of instance attributes, and identifies the superseded design in `database_spec.md`. This transforms the evidence from "we didn't find usage" to "usage was deliberately removed for a documented architectural reason."

REWARD_HACK_DETECTED: no
