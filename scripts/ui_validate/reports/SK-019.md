        # Validation Report: SK-019 — DYN_SKILL_INSTRUCTION

        **Category**: state_keys
        **Validated**: 2026-03-23 00:36
        **Verdict**: PASS

        ---

        ## Validation Report: SK-019

### Summary
**PASS** — All three lifecycle phases (constant definition + curated capture, orchestrator seeding, template resolution, and dispatch restoration) are fully implemented with correct state mutation discipline.

### Evidence

**Phase A: Constant definition & CURATED_STATE_KEYS membership**
- `DYN_SKILL_INSTRUCTION = "skill_instruction"` defined at `rlm_adk/state.py:34`
- `DYN_SKILL_INSTRUCTION` is a member of `CURATED_STATE_KEYS` frozenset at `rlm_adk/state.py:212`
- Additionally captured by the `"skill_"` prefix in `CURATED_STATE_PREFIXES` at `rlm_adk/state.py:219` (belt-and-suspenders)

**Phase B: Template placeholder presence**
- `{skill_instruction?}` appears in `RLM_DYNAMIC_INSTRUCTION` at `rlm_adk/utils/prompts.py:113`
- The `?` suffix makes it optional (ADK resolves to empty string if unset), so no error when no instruction_router is configured
- ADK's dynamic instruction resolver substitutes session state values matching the placeholder name

**Phase C: Orchestrator seeding**
- `DYN_SKILL_INSTRUCTION` imported at `rlm_adk/orchestrator.py:39`
- Conditional seeding at `orchestrator.py:405-408`: `if self.instruction_router is not None:` → calls `self.instruction_router(self.depth, self.fanout_idx)` → if non-None result, sets `initial_state[DYN_SKILL_INSTRUCTION] = _skill_text`
- `initial_state` is yielded as `EventActions(state_delta=initial_state)` at `orchestrator.py:493-497` — correct AR-CRIT-001 compliant mutation path
- `_seed_skill_instruction` before_agent_callback defined at `orchestrator.py:415-421`: writes `callback_context.state[DYN_SKILL_INSTRUCTION] = _skill_text` — also AR-CRIT-001 compliant (callback_context.state)
- Callback wired onto `reasoning_agent` via `object.__setattr__` at `orchestrator.py:423-427`
- Cleanup in `finally` block at `orchestrator.py:665-669` resets `before_agent_callback` to `None`

**Phase D: Dispatch restoration (post_dispatch_state_patch_fn)**
- `DYN_SKILL_INSTRUCTION` imported at `rlm_adk/dispatch.py:32`
- Parent skill instruction captured at `dispatch.py:147-149`: `_parent_skill_instruction = instruction_router(depth, _parent_fanout_idx)` when `instruction_router is not None`
- `post_dispatch_state_patch_fn()` at `dispatch.py:537-546`: returns `{DYN_SKILL_INSTRUCTION: _parent_skill_instruction}` if `_parent_skill_instruction is not None`, else empty dict
- This function is returned as the 3rd element of the tuple at `dispatch.py:548-552`
- Orchestrator passes it to `REPLTool` at `orchestrator.py:321` as `post_dispatch_state_patch_fn=post_dispatch_state_patch_fn`

**Phase E: No-router empty-dict behavior**
- When `instruction_router is None` (dispatch.py:148), `_parent_skill_instruction` stays `None` (initialized at line 147)
- `post_dispatch_state_patch_fn()` returns empty dict `{}` since the `if _parent_skill_instruction is not None` guard at line 544 is False

### Bugs Found
No bugs found. The implementation is complete and correct across all phases.

### Validation Notes
This validation confirmed the complete 3-phase lifecycle of `DYN_SKILL_INSTRUCTION`: (1) the constant is defined as `"skill_instruction"` and included in `CURATED_STATE_KEYS` for sqlite_tracing capture, (2) the orchestrator seeds it via both `EventActions(state_delta=...)` and a `before_agent_callback` using `callback_context.state` — both AR-CRIT-001 compliant mutation paths, (3) ADK's dynamic instruction resolver picks it up via the `{skill_instruction?}` placeholder in `RLM_DYNAMIC_INSTRUCTION`, and (4) `post_dispatch_state_patch_fn` in dispatch.py correctly restores the parent's skill instruction after child orchestrator runs. The test plan's proposed unit test is sound and covers all lifecycle phases, with the caveat that Phase C relies on source inspection rather than runtime execution (acceptable given ADK Runner requirements). The `finally` block cleanup of `before_agent_callback` prevents stale callback leaks across invocations.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The report provides specific, line-number-referenced evidence for each phase of the DYN_SKILL_INSTRUCTION lifecycle: constant definition and CURATED_STATE_KEYS membership, orchestrator seeding via both EventActions(state_delta=...) and callback_context.state (both AR-CRIT-001 compliant), template placeholder presence in the dynamic instruction, and the post_dispatch_state_patch_fn restoration logic in dispatch.py. Critically, the report doesn't just check that names exist — it traces the conditional logic (instruction_router guard), verifies the mutation paths are correct (not ctx.session.state), confirms the finally-block cleanup, and validates the empty-dict fallback when no router is configured. One minor gap: the manifest lists repl_tool.py as a writer "via _patch_fn restoration," but the report only traces as far as the handoff point (orchestrator.py:321 passing the function to REPLTool) without verifying that REPLTool internally calls the patch function and applies results to tool_context.state — however, the supply-side validation (function definition, return value, parameter passing) is thorough enough to be convincing.

REWARD_HACK_DETECTED: no
