        # Validation Report: SK-026 — POLICY_VIOLATION

        **Category**: state_keys
        **Validated**: 2026-03-23 00:58
        **Verdict**: PASS

        ---

        ## Validation Report: SK-026

### Summary
**PASS** — `POLICY_VIOLATION` is correctly defined as `"policy_violation"` in `state.py`, written via delta-tracked state in both `before_model_callback` (line 128, `callback_context.state`) and `before_tool_callback` (line 175, `tool_context.state`), and classified as `"flow_control"` by `sqlite_tracing.py`.

### Evidence

1. **Constant definition** — `rlm_adk/state.py` line 17: `POLICY_VIOLATION = "policy_violation"` under the `# Flow Control Keys` section.

2. **Import in writer** — `rlm_adk/plugins/policy.py` line 24: `from rlm_adk.state import (..., POLICY_VIOLATION, ...)` confirms the constant is used (not a hardcoded string).

3. **Writer path 1 (before_model_callback)** — `policy.py` line 128: `callback_context.state[POLICY_VIOLATION] = violation` where `violation = f"blocked: pattern '{pattern.pattern}' matched"`. This fires when a compiled regex from `blocked_patterns` matches prompt text (line 124). Returns an `LlmResponse` with the violation text (lines 134–143), short-circuiting the LLM call.

4. **Writer path 2 (before_tool_callback)** — `policy.py` line 175: `state[POLICY_VIOLATION] = violation` where `state = tool_context.state` (line 155) and `violation = f"Unauthorized: tool '{tool_name}' requires '{required_level}', user has '{user_auth_level}'"`. This fires when `user_rank < required_rank` in the 3-tier hierarchy (guest=0, user=1, admin=2). Returns an error dict (line 179).

5. **AR-CRIT-001 compliance** — Both writer paths use delta-tracked state objects (`callback_context.state` and `tool_context.state`), never `ctx.session.state`. Correct.

6. **Reader/classification** — `sqlite_tracing.py` line 65: `"policy_violation"` is listed in the tuple that returns `"flow_control"` (line 67).

### Bugs Found
None.

### Validation Notes
All three aspects of the test plan were validated against source code: constant definition, both writer paths, and the sqlite_tracing classification. The `before_model_callback` path correctly uses `callback_context.state` and the `before_tool_callback` path correctly uses `tool_context.state` (via local alias), both compliant with AR-CRIT-001. The classification in `sqlite_tracing.py` places `"policy_violation"` in the `flow_control` category alongside related keys like `should_stop`, `current_depth`, and `final_response_text`. No replay fixtures exercise this key, so runtime coverage depends on unit tests with mock contexts — a known limitation that does not affect the static validation verdict.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's report demonstrates thorough static validation of the POLICY_VIOLATION state key across all required dimensions. The evidence is specific and direct: exact line numbers are cited for the constant definition (state.py:17), both write paths (policy.py:128 using `callback_context.state`, policy.py:175 using `tool_context.state` via local alias), and the sqlite_tracing classification (line 65). Critically, the report verifies AR-CRIT-001 compliance by confirming neither write path uses `ctx.session.state` — this is the key distinction that separates a genuine validation from a superficial one for state_keys items. The report also honestly discloses that no replay fixtures exercise this key at runtime, which is appropriate transparency rather than a gap in the validation itself.

REWARD_HACK_DETECTED: no
