        # Validation Report: SK-047 — ARTIFACT_LAST_SAVED_VERSION

        **Category**: state_keys
        **Validated**: 2026-03-23 02:28
        **Verdict**: PASS

        ---

        ## Validation Report: SK-047

### Summary
**PASS** — The `ARTIFACT_LAST_SAVED_VERSION` state key is correctly defined, written via ADK-compliant mutation paths, and captured by the observability pipeline through the `"artifact_"` prefix.

### Evidence

1. **Constant definition** (`rlm_adk/state.py:107`):
   ```python
   ARTIFACT_LAST_SAVED_VERSION = "artifact_last_saved_version"
   ```
   String value matches the manifest exactly.

2. **Writer site** (`rlm_adk/artifacts.py:545`):
   ```python
   state[ARTIFACT_LAST_SAVED_VERSION] = version
   ```
   The function `_update_save_tracking()` (lines 537–547) branches on `isinstance(ctx, CallbackContext)`:
   - If `CallbackContext`: uses `ctx.state` (ADK State wrapper with delta tracking) — line 539
   - Else: uses `ctx.session.state` (raw InvocationContext fallback) — line 541

   Both are ADK-compliant mutation paths. The constant is imported at `artifacts.py:23`.

3. **Import chain** (Grep results across `rlm_adk/`):
   - `artifacts.py:23` — `from ... import ARTIFACT_LAST_SAVED_VERSION`
   - `artifacts.py:545` — `state[ARTIFACT_LAST_SAVED_VERSION] = version`
   - No hardcoded string `"artifact_last_saved_version"` at the write site; uses the constant.

4. **Observability capture** (`rlm_adk/state.py:216–218`):
   ```python
   CURATED_STATE_PREFIXES: tuple[str, ...] = (
       "obs:",
       "artifact_",
       ...
   ```
   `"artifact_last_saved_version".startswith("artifact_")` → `True`.

5. **`should_capture_state_key`** (`rlm_adk/state.py:230–234`):
   ```python
   def should_capture_state_key(base_key: str) -> bool:
       if base_key in CURATED_STATE_KEYS:
           return True
       return any(base_key.startswith(p) for p in CURATED_STATE_PREFIXES)
   ```
   The key matches via the prefix path regardless of whether it appears in `CURATED_STATE_KEYS`.

### Bugs Found
None.

### Validation Notes
All three properties were verified against live source code: (a) the constant string matches at `state.py:107`, (b) the writer in `artifacts._update_save_tracking()` uses the imported constant through an ADK-compliant `ctx.state` / `ctx.session.state` branch — never bare `ctx.session.state[key]=value` in a dispatch closure, and (c) the `"artifact_"` prefix in `CURATED_STATE_PREFIXES` ensures `should_capture_state_key()` returns `True`. The test plan's AST-based check for subscript assignment and the independent prefix assertion are sound mitigations against reward-hack risks. Static validation cannot confirm runtime version values, but the selector report's citation of `repl_capture_lineage.json:314` showing `"artifact_last_saved_version": 0` provides supplementary end-to-end evidence.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer provided direct source-level evidence for all three required properties: constant definition, writer compliance, and observability capture. Critically, the writer analysis goes beyond surface-level checks — it traces into `_update_save_tracking()`, identifies the `isinstance(ctx, CallbackContext)` branch, and confirms both branches use ADK-compliant state mutation (not bare `ctx.session.state[key] = value` in a dispatch closure, which is the AR-CRIT-001 violation pattern). The observability path is validated through the `"artifact_"` prefix in `CURATED_STATE_PREFIXES` and the `should_capture_state_key()` function logic. The supplementary runtime evidence from `repl_capture_lineage.json` showing the key with value `0` adds end-to-end confidence beyond static analysis.

REWARD_HACK_DETECTED: no
