        # Validation Report: DI-003 — skill_instruction template var

        **Category**: dynamic_instructions
        **Validated**: 2026-03-23 03:30
        **Verdict**: PASS

        ---

        All evidence gathered. Here is the validation report:

## Validation Report: DI-003

### Summary
**PASS** — The `skill_instruction` dynamic instruction variable is fully wired: constant defined, curated, template placeholder present, orchestrator seeds via legal mutation paths (AR-CRIT-001 compliant), and dispatch restores the parent value after child dispatch.

### Evidence

| Checkpoint | Evidence | File & Line |
|---|---|---|
| **(a) Constant definition** | `DYN_SKILL_INSTRUCTION = "skill_instruction"` | `rlm_adk/state.py:34` |
| **(a) CURATED membership** | `DYN_SKILL_INSTRUCTION` is an explicit member of `CURATED_STATE_KEYS` frozenset | `rlm_adk/state.py:212` |
| **(b) Template placeholder** | `{skill_instruction?}` appears in `RLM_DYNAMIC_INSTRUCTION` string on the line `Skill instruction: {skill_instruction?}` | `rlm_adk/utils/prompts.py:113` |
| **(c) initial_state seed** | `initial_state[DYN_SKILL_INSTRUCTION] = _skill_text` — dict literal assignment (legal, not session state write) | `rlm_adk/orchestrator.py:408` |
| **(c) callback_context seed** | `callback_context.state[DYN_SKILL_INSTRUCTION] = _skill_text` inside `_seed_skill_instruction` async closure — AR-CRIT-001 compliant via `callback_context.state` | `rlm_adk/orchestrator.py:420` |
| **(c) No AR-CRIT-001 violation** | Grep for `ctx.session.state[.*skill_instruction` in orchestrator.py returned **no matches** | `rlm_adk/orchestrator.py` (full file) |
| **(d) Parent capture** | `_parent_skill_instruction = instruction_router(depth, _parent_fanout_idx)` | `rlm_adk/dispatch.py:149` |
| **(d) Post-dispatch restoration** | `delta[DYN_SKILL_INSTRUCTION] = _parent_skill_instruction` inside `post_dispatch_state_patch_fn()` guarded by `if _parent_skill_instruction is not None` | `rlm_adk/dispatch.py:544-545` |

### Bugs Found
None.

### Validation Notes
All four checkpoints pass with direct source-level evidence. The constant `DYN_SKILL_INSTRUCTION` is defined at `state.py:34`, included in `CURATED_STATE_KEYS` at line 212, and the `{skill_instruction?}` optional template placeholder at `prompts.py:113` uses ADK's `{var?}` syntax for runtime state-variable injection. The orchestrator writes exclusively through `initial_state` (dict literal) and `callback_context.state` (the `_seed_skill_instruction` before-agent callback), with zero `ctx.session.state` violations confirmed by grep. The dispatch restoration round-trip (`_parent_skill_instruction` capture at line 149, delta restoration at line 545) ensures the parent's skill instruction survives child dispatch overwriting. The one caveat is that ADK's actual template engine resolution at runtime is not exercised by static analysis — an integration/replay test would be needed to prove end-to-end rendering, but the wiring is structurally complete and correct.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence is convincing because it traces the full lifecycle of the `skill_instruction` state key through four distinct checkpoints with specific file/line citations: definition and curation membership in `state.py`, the `{skill_instruction?}` template placeholder in `prompts.py`, two legal mutation paths in `orchestrator.py` (dict literal seed and `callback_context.state` write), and the parent-value capture/restoration round-trip in `dispatch.py`. Critically, the report goes beyond "the constant exists" — it verifies the *mutation paths* are AR-CRIT-001 compliant by grepping for illegal `ctx.session.state` writes and confirming zero matches. The Implementer transparently acknowledges the one limitation (no runtime template engine test), which is appropriate honesty rather than a gap being hidden.

REWARD_HACK_DETECTED: no
