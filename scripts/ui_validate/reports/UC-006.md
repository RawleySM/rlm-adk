        # Validation Report: UC-006 — Model calls metric chip

        **Category**: ui_controls
        **Validated**: 2026-03-23 03:54
        **Verdict**: PASS

        ---

        ## Validation Report: UC-006

### Summary
**PASS** — The full data pipeline for the "model calls" metric chip is correctly implemented: telemetry counting in the loader, field propagation through data models and controller, and rendering as a styled pill chip in the header.

### Evidence

| # | Aspect | Evidence | File |
|---|--------|----------|------|
| 1 | **Data model (stats)** | `total_live_model_calls: int = 0` at line 267, inside `@dataclass(frozen=True) class LiveRunStats` | `live_models.py:267` |
| 2 | **Data model (state)** | `total_live_model_calls: int` at line 293, inside `@dataclass(frozen=True) class LiveRunState` (no default — required field) | `live_models.py:293` |
| 3 | **Loader counting** | `total_live_model_calls=sum(1 for row in telemetry if row.get("event_type") == "model_call")` at lines 816-817, inside `LiveRunStats` constructor in `_build_snapshot` | `live_loader.py:816-817` |
| 4 | **Controller propagation** | `total_live_model_calls=self.state.stats.total_live_model_calls` at line 689, inside the `LiveRunState` constructor in the controller's state-building method | `live_controller.py:689` |
| 5 | **Renderer read** | `total_calls = run_state.total_live_model_calls if run_state else 0` at line 221 — safely defaults to `0` when `run_state` is `None` | `live_app.py:221` |
| 6 | **Chip invocation** | `_metric_chip("model calls", str(total_calls))` at line 224 — passes the label `"model calls"` and stringified count | `live_app.py:224` |
| 7 | **Chip renderer** | `def _metric_chip(label: str, value: str)` at line 548. Creates a pill-shaped div (`border-radius: 999px`) with two `ui.label` elements: one for the label text (styled `--text-1`, `0.72rem`) and one for the value (styled `--text-0`, `0.74rem`) | `live_app.py:548-555` |
| 8 | **Zero-call safety** | `LiveRunStats` default `total_live_model_calls=0` (line 267) + renderer guard `if run_state else 0` (line 221) ensure "0" is displayed when no model calls exist | `live_models.py:267`, `live_app.py:221` |

### Bugs Found
None.

### Validation Notes
All seven layers of the data pipeline were verified: field declaration in both `LiveRunStats` (with default `0`) and `LiveRunState` (required), telemetry counting via generator expression filtered on `event_type == "model_call"`, controller passthrough from `self.state.stats`, safe read with `None` guard in the renderer, correct label/value arguments to `_metric_chip`, and the chip function's two-label pill structure. The zero-call edge case is handled both by the dataclass default and by the renderer's `if run_state else 0` guard. Static validation confirms the wiring is complete and consistent; the only caveat is that we cannot verify the visual rendering (font sizes, colors, pill shape) without a live screenshot, but the CSS properties in code match the expected pill chip design.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
Every claim in the implementer's report was verified against the actual source code at the cited line numbers. The `total_live_model_calls` field exists in both `LiveRunStats` (line 267, with default `0`) and `LiveRunState` (line 293, required). The loader correctly counts telemetry rows where `event_type == "model_call"` via a generator sum (lines 816-817). The controller propagates `self.state.stats.total_live_model_calls` into the `LiveRunState` constructor (line 689). The renderer reads the field with a safe `None` guard (line 221) and passes it to `_metric_chip("model calls", str(total_calls))` (line 224). The `_metric_chip` function (lines 548-555) renders a pill-shaped div with two `ui.label` elements for label and value. The evidence is direct code inspection across all pipeline layers, not circumstantial.

REWARD_HACK_DETECTED: no
