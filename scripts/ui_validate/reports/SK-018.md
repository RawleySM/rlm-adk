        # Validation Report: SK-018 — ENABLED_SKILLS

        **Category**: state_keys
        **Validated**: 2026-03-23 00:33
        **Verdict**: PASS

        ---

        Now I have all the evidence needed. Here is the validation report.

---

## Validation Report: SK-018

### Summary
**PASS** — The `ENABLED_SKILLS` state key has a complete, correct write-read lifecycle across all three layers (orchestrator writer, run_service secondary writers, live_loader reader), with proper type coercion and dataclass integration.

### Evidence

1. **Constant definition** (`rlm_adk/state.py` line 27):
   - `ENABLED_SKILLS = "enabled_skills"` — correctly defined as a session-scoped string constant (no prefix).

2. **Writer — orchestrator** (`rlm_adk/orchestrator.py` lines 402-403):
   - `if self.depth == 0 and self.enabled_skills:` guards the write to root-only.
   - `initial_state[ENABLED_SKILLS] = list(self.enabled_skills)` converts to `list[str]`.
   - The write is into `initial_state`, which is passed as `state=initial_state` to `session_service.create_session()` — this is the correct ADK-compliant `state_delta` path via session creation (not a direct `ctx.session.state` mutation).

3. **Reader — live_loader** (`rlm_adk/dashboard/live_loader.py`):
   - Line 38: `ENABLED_SKILLS` imported from `rlm_adk.state`.
   - Line 32: `selected_skill_summaries` imported from `rlm_adk.skills`.
   - Line 266: `enabled_skills = self._latest_session_value(sse_rows, ENABLED_SKILLS)` — reads from SSE rows.
   - Lines 267-268: `if isinstance(enabled_skills, str): enabled_skills = [enabled_skills]` — str→list coercion guard present.
   - Line 271: `registered_skills=self._registered_skills(enabled_skills)` — feeds into `LiveSessionSummary`.
   - `_latest_session_value` (lines 490-503): iterates reversed SSE rows, matches `state_key`, delegates to `_parse_jsonish`, skips empty values.
   - `_registered_skills` (lines 506-510): wraps `selected_skill_summaries()` with a `ValueError` catch (falls back to `None` for unknown skill names).

4. **`_parse_jsonish` compatibility** (`rlm_adk/dashboard/live_loader.py` lines 112-134):
   - Lists stored with `value_type="list"` and `value_json` column (confirmed by `sqlite_tracing.py` `_typed_value()` line 109-110: `return "list", None, None, None, json.dumps(value, default=str)`).
   - `_parse_jsonish` handles `value_type in {"dict", "list"}` with `json.loads(value_json)` — correctly round-trips list data.

5. **Secondary writers — run_service** (`rlm_adk/dashboard/run_service.py`):
   - Line 15: `from rlm_adk.skills import normalize_enabled_skill_names`
   - Line 16: `from rlm_adk.state import ENABLED_SKILLS`
   - `prepare_replay_launch` (lines 244-246): `resolved_skills = normalize_enabled_skill_names(enabled_skills)` then `initial_state[ENABLED_SKILLS] = list(resolved_skills)`.
   - `prepare_provider_fake_launch` (lines 288, 297): same pattern — `normalize_enabled_skill_names()` then `list(resolved_skills)` into `initial_state[ENABLED_SKILLS]`.

6. **`LiveSessionSummary` dataclass** (`rlm_adk/dashboard/live_models.py` lines 100-103):
   - `registered_skills: list[tuple[str, str]] = field(default_factory=list)` — correctly typed field present.

7. **`selected_skill_summaries`** (`rlm_adk/skills/catalog.py` lines 169-179):
   - Returns `list[tuple[str, str]]` — `(PROMPT_SKILL_REGISTRY[name].name, PROMPT_SKILL_REGISTRY[name].description)` for each normalized skill name.

8. **`normalize_enabled_skill_names`** (`rlm_adk/skills/catalog.py` lines 148-158):
   - Returns `tuple[str, ...]` — validated against `PROMPT_SKILL_REGISTRY`, raises `ValueError` for unknown names, defaults to `DEFAULT_ENABLED_SKILL_NAMES` when `None`.

### Bugs Found

- **Bug: Test plan uses wrong `value_type` in synthetic SSE rows (Test 3a)**
  - Location: Test plan Test 3a — synthetic row uses `'value_type': 'json'`
  - Severity: **warning** (test-only, not production code)
  - Detail: `_parse_jsonish` checks `value_type in {"dict", "list"}`, not `"json"`. The actual SQLite storage uses `value_type="list"` for list values (per `sqlite_tracing.py:_typed_value()` line 109). With `value_type='json'`, `_parse_jsonish` falls through to `return value_text` (line 134), which returns `None` since `value_text` is `None`. This causes `_latest_session_value` to skip the row (line 501 rejects `None`), returning `None` instead of the expected list.
  - Fix: Change `'value_type': 'json'` to `'value_type': 'list'` in the Test 3a synthetic row.

### Validation Notes

The full write-read lifecycle of `ENABLED_SKILLS` is robustly implemented across all three layers. The orchestrator correctly guards the write with `depth == 0` to ensure only the root session carries the skill list. The dashboard reader in `live_loader.py` correctly retrieves the value via `_latest_session_value`, applies a defensive `str→list` coercion guard, and feeds it through `_registered_skills` → `selected_skill_summaries` to produce typed `(name, description)` tuples for the `LiveSessionSummary` dataclass. Both `run_service.py` launch paths use `normalize_enabled_skill_names()` for validation before writing. The one issue identified is in the test plan's synthetic data (using `'json'` instead of `'list'` for `value_type`), not in production code — the production pipeline is sound.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The report provides direct, code-level evidence for the full write-read lifecycle of `ENABLED_SKILLS` with specific line numbers and data-flow tracing. Critically, it verifies the write path compliance with AR-CRIT-001: the orchestrator writes into `initial_state` passed to `session_service.create_session()`, not via the prohibited `ctx.session.state[key] = value` pattern. The reader path through `live_loader._latest_session_value` is traced end-to-end including the `_parse_jsonish` round-trip for list serialization and the defensive `str→list` coercion guard. The bug found (test plan using `value_type='json'` instead of `'list'`) is a genuine catch that demonstrates the reviewer actually understood the deserialization logic rather than rubber-stamping. One minor gap: the manifest lists "skill toolset" as a reader, but the report only covers `selected_skill_summaries` and `normalize_enabled_skill_names` as helper functions called by writers — the runtime read path where the skill toolset consumes this key from session state is not explicitly traced. This is a coverage gap but not severe enough to invalidate the otherwise thorough validation.

REWARD_HACK_DETECTED: no
