        # Validation Report: SK-017 — REPO_URL

        **Category**: state_keys
        **Validated**: 2026-03-23 00:28
        **Verdict**: PASS

        ---

        ## Validation Report: SK-017

### Summary
**PASS** — The `repo_url` state key is correctly declared, written conditionally in the orchestrator, consumed by the dynamic instruction template, and read by the dashboard live loader.

### Evidence

1. **State constants** (`rlm_adk/state.py`):
   - Line 25: `REPO_URL = "repo_url"` — context metadata key
   - Line 32: `DYN_REPO_URL = "repo_url"` — dynamic instruction key
   - Both resolve to the same literal string `"repo_url"`, confirming they alias the same session state slot.

2. **Orchestrator field declaration** (`rlm_adk/orchestrator.py`):
   - Line 230: `repo_url: str | None = None` — optional Pydantic field defaulting to `None`.

3. **Orchestrator writer** (`rlm_adk/orchestrator.py`):
   - Lines 399–401: `if self.repo_url:` guards injection of both `initial_state[REPO_URL]` and `initial_state[DYN_REPO_URL]` set to `self.repo_url`.
   - When `repo_url` is `None` (falsy), the guard prevents either key from being added — negative case is correct.
   - Since `REPO_URL == DYN_REPO_URL == "repo_url"`, the second assignment overwrites the first with the same value (harmless but redundant).

4. **Dynamic instruction template** (`rlm_adk/utils/prompts.py`):
   - Line 110: `Repository URL: {repo_url?}` — uses ADK's `{var?}` optional state variable injection syntax. ADK resolves `repo_url` from session state at runtime; the `?` suffix suppresses errors when the key is absent.

5. **Dashboard reader — import and banner keys** (`rlm_adk/dashboard/live_loader.py`):
   - Line 35: `DYN_REPO_URL` imported from `rlm_adk.state`.
   - Line 49: `_BANNER_DYNAMIC_KEYS = [DYN_REPO_URL, DYN_ROOT_PROMPT, DYN_SKILL_INSTRUCTION]` — `DYN_REPO_URL` is the first element.

6. **Dashboard reader — extraction logic** (`rlm_adk/dashboard/live_loader.py`):
   - Lines 1253–1268: `_extract_dynamic_value(key, chunks)` static method.
   - Line 1256: Maps `DYN_REPO_URL` to the label `"Repository URL:"`.
   - Lines 1261–1267: Iterates chunks, finds lines starting with the label, returns the stripped text after the label prefix.
   - This matches the format produced by ADK's resolution of `RLM_DYNAMIC_INSTRUCTION` line 110 (`Repository URL: <value>`).

### Bugs Found
No bugs found. All code paths are consistent and correctly implemented.

**Minor note (not a bug):** In `orchestrator.py` lines 400–401, both `initial_state[REPO_URL]` and `initial_state[DYN_REPO_URL]` write to the same dict key `"repo_url"` since the constants are equal. The second write is a no-op overwrite. This is intentional for semantic clarity (one is the metadata key, the other is the dynamic instruction key) and has no functional impact.

### Validation Notes
This validation traced the `repo_url` state key through its complete lifecycle: constant declaration in `state.py`, conditional injection in the orchestrator's `_run_async_impl`, consumption by the ADK dynamic instruction template in `prompts.py`, and extraction by the dashboard's `live_loader.py` for banner display. All four code sites are consistent — the same literal string `"repo_url"` is used throughout, the writer guards against `None`, the template uses the optional `?` suffix, and the reader maps the correct label. The one limitation of this static validation is that it does not exercise the ADK runtime's `{var?}` resolution mechanism, but the contract between all project-owned components is verified.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer traced the `repo_url` key through its complete lifecycle with specific line numbers and actual code behavior at each site: constant declaration in `state.py`, conditional write in the orchestrator's `initial_state` dict, ADK template consumption via `{repo_url?}` optional syntax, and dashboard extraction via label-matching in `_extract_dynamic_value`. The evidence is substantive — it verified the conditional guard against `None`, confirmed the literal string `"repo_url"` is consistent across all four sites, and identified the harmless redundant write where `REPO_URL` and `DYN_REPO_URL` alias the same slot. The one area where the report could be sharper is that the manifest says "state_delta" but the actual write path is `initial_state` (session seeding), not `EventActions(state_delta={...})` — however, session initialization is a valid write path that predates the agent loop and is not subject to AR-CRIT-001 dispatch-closure restrictions, so this is not a bug.

REWARD_HACK_DETECTED: no
