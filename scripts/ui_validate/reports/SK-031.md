        # Validation Report: SK-031 — OBS_REWRITE_FAILURE_COUNT

        **Category**: state_keys
        **Validated**: 2026-03-23 01:19
        **Verdict**: PASS

        ---

        ## Validation Report: SK-031

### Summary
**PASS** — `OBS_REWRITE_FAILURE_COUNT` is a confirmed dead-letter state key: defined in `state.py`, listed in `live_loader.py`'s `_KNOWN_OBS_KEYS`, tracked as an instance attribute on `REPLTool`, but **never written to `tool_context.state`**. The dashboard gracefully skips absent keys.

### Evidence

| # | Claim | Evidence | File |
|---|-------|----------|------|
| A | Constant defined with correct value | `OBS_REWRITE_FAILURE_COUNT = "obs:rewrite_failure_count"` at line 67 | `rlm_adk/state.py` |
| B | Instance attr initialized | `self._rewrite_failure_count = 0` at line 94 | `rlm_adk/tools/repl_tool.py` |
| C | Instance attr incremented on AST rewrite failure | `self._rewrite_failure_count += 1` at line 233, inside `except Exception` block after `rewrite_for_async()` / `compile()` fails | `rlm_adk/tools/repl_tool.py` |
| D | **No `tool_context.state` write anywhere** | Grep for `tool_context\.state\[.*rewrite_failure` across all of `rlm_adk/` → **zero matches**. Grep for `state\[.*rewrite_failure` → **zero matches**. | `rlm_adk/` (whole tree) |
| E | Dashboard reader lists the key | `"obs:rewrite_failure_count"` appears in `_KNOWN_OBS_KEYS` list at line 67 | `rlm_adk/dashboard/live_loader.py` |
| F | Dashboard tolerates absent key | Lines 389–391: `item = state_lookup.get(key)` / `if item is None: continue` — uses `.get()` (no `KeyError`) and skips `None` results | `rlm_adk/dashboard/live_loader.py` |

### Bugs Found

- **Bug**: `OBS_REWRITE_FAILURE_COUNT` is never written to session state — the instance attribute `self._rewrite_failure_count` is incremented but never flushed to `tool_context.state`. The sibling keys `OBS_REWRITE_COUNT` and `OBS_REWRITE_TOTAL_MS` likely have the same gap (they also use instance attrs `self._rewrite_count` and `self._rewrite_total_ms`), but that is outside this item's scope.
- **Location**: `rlm_adk/tools/repl_tool.py`, around line 233 (the increment site) — a corresponding `tool_context.state[OBS_REWRITE_FAILURE_COUNT] = self._rewrite_failure_count` write is missing.
- **Severity**: warning — the dashboard won't crash (the `None` guard handles it), but AST rewrite failure telemetry is silently invisible in the live dashboard. The data is collected but never surfaced.
- **Fix**: Add a state write in `REPLTool`'s post-execution path (e.g., alongside other obs key writes) that flushes `self._rewrite_failure_count` to `tool_context.state[OBS_REWRITE_FAILURE_COUNT]`. Alternatively, if the key is intentionally deprecated, remove it from both `state.py` and `live_loader.py`'s `_KNOWN_OBS_KEYS` to eliminate the dead code.

### Validation Notes

All three sub-checks from the test plan pass. The constant is correctly defined in `state.py:67`, the dashboard reader at `live_loader.py:388-391` safely skips absent obs keys via `.get()` + `None` guard, and a comprehensive grep of the entire `rlm_adk/` tree confirms zero code paths write this key to `tool_context.state` or any other state surface. The instance attribute at `repl_tool.py:94,233` proves the value **is** tracked at runtime but remains stranded on the `REPLTool` object instance, never reaching session state where the dashboard could read it. This is a telemetry gap, not a crash risk.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's report demonstrates thorough, non-trivial validation across all relevant dimensions. Evidence D is the critical piece — a comprehensive grep confirming zero state writes for this key anywhere in the codebase — which directly validates the manifest's claim that "state write not implemented." The report goes beyond surface-level checks: it traces the full lifecycle from constant definition (state.py:67) through instance attribute initialization (repl_tool.py:94) and increment (repl_tool.py:233) to the absent flush path, and independently confirms the dashboard's graceful degradation via the `.get()` + `None` guard pattern (live_loader.py:389-391). The bug finding is accurately scoped and severity-rated as warning rather than being swept under the rug — the data is collected but never surfaced, which is exactly the gap the manifest describes. The sibling key observation shows genuine analytical depth without scope creep.

REWARD_HACK_DETECTED: no
