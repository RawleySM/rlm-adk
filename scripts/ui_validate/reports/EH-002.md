        # Validation Report: EH-002 — on_launch

        **Category**: event_handlers
        **Validated**: 2026-03-23 06:24
        **Verdict**: PASS

        ---

        ## Validation Report: EH-002

### Summary
**PASS** — The `on_launch` event handler is correctly wired: the Launch button's `on_click` invokes `on_launch()` which calls `controller.launch_replay()`, and the controller implements all required state transitions (happy path, re-entrancy guard, no-fixture error path).

### Evidence

**Wiring (live_app.py)**
- `async def on_launch()` defined at line 388, calls `await controller.launch_replay()` then `live_ui.refresh_all()` (lines 388–390)
- Launch button created at line 429: `ui.button(launch_label, on_click=on_launch)` — binding confirmed
- Conditional rendering: when `launch_in_progress` is `True`, a Cancel button is shown instead (lines 421–427); the Launch button only appears in the `else` branch (lines 428–434)

**Re-entrancy guard (live_controller.py:91–93)**
- `launch_replay()` checks `if self.state.launch_in_progress: return None` at the top — early exit with no side effects

**No-fixture error path (live_controller.py:106–108)**
- When neither `selected_provider_fake_fixture` nor `replay_path` is set, `self.state.launch_error = "No fixture selected"` and returns `None`
- `launch_in_progress` is only set to `True` at line 113, which is after this early return — so it stays `False`

**Happy path state transitions (live_controller.py:95–122)**
- Calls `prepare_provider_fake_launch()` or `prepare_replay_launch()` depending on which fixture is set (lines 96–105)
- Sets `launch_in_progress = True` (line 113)
- Resets `launch_cancelled = False` (line 114)
- Clears `launch_error = None` (line 115)
- Populates `launched_session_id = handle.session_id` (line 116)
- Prepends session to `available_sessions` with dedup check (lines 117–118)
- Sets a default session label via `setdefault` (line 119)
- Calls `await self.select_session(handle.session_id)` (line 120)
- Spawns background task: `self._launch_task = asyncio.create_task(self._run_launch(handle))` (line 121)
- Returns `handle.session_id` (line 122)

**Error handling in _run_launch (live_controller.py:124–133)**
- `_run_launch` catches non-cancellation exceptions, sets `launch_error` and `last_error`
- `finally` block resets `launch_in_progress = False` — ensures the guard is released even on failure

**Launch handle contracts (run_service.py:23–52)**
- `ReplayLaunchHandle` is a frozen dataclass with `session_id: str` and `async def run()` — matches what the controller expects
- `ProviderFakeLaunchHandle` follows the same interface pattern

### Bugs Found
No bugs found. All described behaviors are implemented correctly.

### Validation Notes
Static analysis confirms all four aspects of the EH-002 contract: (1) the `on_launch` function is defined and bound to the Launch button via `on_click`, (2) it delegates to `controller.launch_replay()` and refreshes the UI, (3) the controller implements a proper state machine with re-entrancy guard, error path, and happy-path transitions, and (4) the background task is spawned with cleanup semantics. The test plan's proposed unit tests accurately target these code paths. One caveat: the `enabled_skills` passthrough from `self.state.selected_skills` to `prepare_*_launch` (lines 99, 104) is present but not explicitly called out in the manifest item — it works correctly based on the code.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer's report provides direct, line-numbered evidence for every claim: the `on_launch` function definition and its binding to the button's `on_click` at specific lines, the delegation to `controller.launch_replay()`, the re-entrancy guard with early return, the no-fixture error path, and the full happy-path state machine including background task spawning and cleanup in the `finally` block. The evidence is structural and behavioral — not just "the function exists" but "here is what it does, in what order, with what guards." The report traces the full call chain from UI button through controller logic to background task lifecycle, which is exactly what the manifest item requires ("Fired when Launch button clicked. Calls controller.launch_replay()"). The frozen dataclass launch handle contracts are also verified against what the controller consumes. This is a thorough validation with no gaps between what was claimed and what was shown.

REWARD_HACK_DETECTED: no
