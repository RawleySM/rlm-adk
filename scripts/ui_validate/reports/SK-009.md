        # Validation Report: SK-009 — REPL_SUBMITTED_CODE_CHARS

        **Category**: state_keys
        **Validated**: 2026-03-23 00:01
        **Verdict**: PASS

        ---

        ## Validation Report: SK-009

### Summary
**PASS** — The `REPL_SUBMITTED_CODE_CHARS` state key is correctly defined, depth-scoped, written as `len(code)` via `tool_context.state` in REPLTool, persisted through the sqlite_tracing capture gate with category `"repl"`, and consumed generically by the dashboard's `LiveStateItem` pipeline.

### Evidence

1. **Constant definition** — `rlm_adk/state.py:78`: `REPL_SUBMITTED_CODE_CHARS = "repl_submitted_code_chars"`. String value matches the manifest exactly.

2. **Depth-scoped membership** — `rlm_adk/state.py:154`: `REPL_SUBMITTED_CODE_CHARS` is a member of `DEPTH_SCOPED_KEYS` set, confirming keys are suffixed with `@dN` at depth > 0 via `depth_key()`.

3. **Write site** — `rlm_adk/tools/repl_tool.py:126`: `tool_context.state[depth_key(REPL_SUBMITTED_CODE_CHARS, self._depth)] = len(code)`. Correctly writes an integer (character count) via `tool_context.state` (compliant with AR-CRIT-001), using `depth_key()` for depth-scoping.

4. **Capture gate** — `rlm_adk/plugins/sqlite_tracing.py:612`: `_should_capture(base_key)` is called on the base key (after `_parse_key` strips `@dN`). `_should_capture` is aliased from `state.should_capture_state_key` at line 45. The key `"repl_submitted_code_chars"` passes because it starts with `"repl_submitted_code"`.

5. **Categorization** — `rlm_adk/plugins/sqlite_tracing.py:68`: `key.startswith("repl_submitted_code")` matches, returning `"repl"` as the category. This is stored in the `key_category` column of `session_state_events`.

6. **Persistence to DB** — `rlm_adk/plugins/sqlite_tracing.py:617-622`: After passing the capture gate, `_typed_value_for_key` extracts typed columns (`value_int` for an integer), and the row is inserted into `session_state_events` with `state_key`, `key_depth`, `key_fanout`, and typed value columns.

7. **Dashboard consumption** — `rlm_adk/dashboard/live_loader.py:632-641`: `_refresh_sse()` queries all `session_state_events` rows for a trace, with no key-specific filter. At lines 957-987, `_latest_state_by_depth()` constructs `LiveStateItem` objects from every SSE row generically — keyed by `(depth, base_key)` tuple. The `value_int` column is parsed via `_parse_jsonish()` (line 964-969), so the integer character count is correctly recovered.

### Bugs Found
None.

### Validation Notes
All five layers of the SK-009 pipeline were validated by direct code inspection: constant definition, depth-scoping membership, write-site expression and mutation path, persistence capture gate + categorization, and generic dashboard consumption. The write site at `repl_tool.py:126` specifically writes `len(code)` (an integer character count, not the code string itself) via the AR-CRIT-001-compliant `tool_context.state` path. The dashboard consumption is generic over all captured state keys — there is no key-specific exclusion that could prevent `repl_submitted_code_chars` from surfacing. The only caveat is that this is static analysis; runtime correctness (e.g., that `self._depth` is correctly initialized) was not exercised, but the write-site pattern is identical to the adjacent `REPL_SUBMITTED_CODE` and `REPL_SUBMITTED_CODE_HASH` writes which share the same `self._depth` value.

        ---

        ## Reviewer Assessment

        Every claim I spot-checked matches the source code exactly:

- **state.py:78** — `REPL_SUBMITTED_CODE_CHARS = "repl_submitted_code_chars"` ✓
- **state.py:154** — present in `DEPTH_SCOPED_KEYS` ✓
- **repl_tool.py:126** — `tool_context.state[depth_key(REPL_SUBMITTED_CODE_CHARS, self._depth)] = len(code)` ✓ (AR-CRIT-001 compliant, writes integer char count)
- **sqlite_tracing.py:45** — `_should_capture = should_capture_state_key` ✓
- **sqlite_tracing.py:68** — `key.startswith("repl_submitted_code")` → `"repl"` category ✓
- **sqlite_tracing.py:611-622** — full persistence pipeline with `_parse_key`, `_should_capture`, `_typed_value_for_key`, INSERT ✓

---

VERDICT: PASS

REASONING:
The Implementer traced every layer of the SK-009 pipeline with precise line-number citations, and every claim I spot-checked against the source code was accurate. Critically, the report did not stop at "the constant is defined" — it verified the actual write expression (`len(code)`), confirmed the mutation goes through `tool_context.state` (AR-CRIT-001 compliant), validated depth-scoping membership, confirmed the sqlite capture gate passes via the `startswith("repl_submitted_code")` prefix match, and traced the generic dashboard consumption path. The report also correctly noted the limitation that this is static analysis only, which is an honest caveat rather than a gap.

REWARD_HACK_DETECTED: no
