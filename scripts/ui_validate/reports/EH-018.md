        # Validation Report: EH-018 — on_open_context

        **Category**: event_handlers
        **Validated**: 2026-03-23 07:28
        **Verdict**: PASS

        ---

        I now have all the evidence needed.

## Validation Report: EH-018

### Summary
**PASS** — The full `on_open_context` event handler chain is correctly implemented across both rendering paths (flow view and tree view), with proper click bindings, argument passing, controller state mutation, and context viewer activation.

### Evidence

1. **`_context_chip()` in `flow_reasoning_pane.py` (lines 133–181)**:
   - `cursor: pointer` applied via inline style at line 171
   - `click.stop` bound at line 176: `el.on("click.stop", lambda _e, i=item, pid=pane_id: on_open_context(pid, i))`
   - Callback receives `(pane_id, item)` — 2-arg form correct for flow view
   - Guard `if on_open_context:` at line 175 prevents binding when no callback provided

2. **`_context_chip()` in `live_invocation_tree.py` (lines 405–456)**:
   - `cursor: pointer` applied via inline style at line 443
   - `click.stop` bound at lines 447–449: `.on("click.stop", lambda _e: on_open_context(invocation, item, lineage))`
   - Callback receives `(invocation, item, lineage)` — 3-arg form correct for tree view
   - Always binds (no guard), since tree view always has a context callback

3. **`_open_flow_context()` in `live_app.py` (lines 812–824)**:
   - Receives `(pane_id, item)` from flow chip callback (line 786)
   - Resolves `invocation` via `controller._pane_by_id(pane_id)` → `controller.selected_invocation(pane)`
   - Resolves `lineage` via `controller.selected_invocation_lineage()`
   - Delegates to `controller.open_invocation_context_viewer(invocation, item, lineage)` at line 823
   - Calls `refresh_viewer.refresh()` to update the UI

4. **`_open_invocation_context_viewer()` in `live_app.py` (lines 672–680)**:
   - Tree view adapter: receives `(invocation, item, lineage)` directly
   - Delegates to `controller.open_invocation_context_viewer(invocation, item, lineage)` at line 679
   - Calls `refresh_viewer.refresh()`

5. **`open_invocation_context_viewer()` in `live_controller.py` (lines 270–288)**:
   - Calls `self.loader.resolve_banner_item_text(invocation, item, lineage=lineage)` at lines 276–279
   - Constructs `LiveContextSelection` with all 5 fields: `label`, `raw_key`, `scope`, `source_kind`, `text` (lines 281–287)
   - Sets `self.state.context_viewer_open = True` at line 288

6. **`LiveContextSelection` dataclass in `live_models.py` (lines 91–96)**:
   - Fields: `label: str`, `raw_key: str`, `scope: str`, `source_kind: SourceKind`, `text: str` — matches all 5 fields populated by the controller

### Bugs Found
None.

### Validation Notes
The full chain was validated across both rendering paths: flow view chips pass `(pane_id, item)` through `_open_flow_context` which resolves the invocation/lineage before delegating to the controller, while tree view chips pass `(invocation, item, lineage)` directly through `_open_invocation_context_viewer`. Both paths converge on `LiveDashboardController.open_invocation_context_viewer()` which calls `resolve_banner_item_text()` and populates all 5 fields of `LiveContextSelection` plus `context_viewer_open=True`. The test plan's Part B assertions about exact callback argument patterns will need minor adjustment — `flow_reasoning_pane` uses `on_open_context(pid, i)` (not `on_open_context(pane_id, i)`) — but the plan already accounts for this with an `or` chain. Static validation cannot confirm NiceGUI's runtime event propagation behavior, but the `click.stop` modifier and lambda wiring are structurally correct.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence directly traces the full event handler chain from click binding to state mutation, not just naming or file existence. The Implementer cited specific lambda expressions (`on_open_context(pid, i)`), method signatures, argument patterns, and the terminal state write (`context_viewer_open = True` plus `LiveContextSelection` population with all 5 fields). Both rendering paths (flow view's 2-arg form and tree view's 3-arg form) were traced to their convergence point in `controller.open_invocation_context_viewer()`. The Implementer was transparent about the limitation that static analysis cannot confirm NiceGUI's runtime event propagation, which is an honest caveat rather than a gap. The manifest's core claim — "fired when context item chip clicked, opens context detail" — is substantiated by the click.stop binding on chips and the controller setting `context_viewer_open = True`.

REWARD_HACK_DETECTED: no
