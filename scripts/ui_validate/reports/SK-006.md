        # Validation Report: SK-006 — REPL_SUBMITTED_CODE

        **Category**: state_keys
        **Validated**: 2026-03-22 23:48
        **Verdict**: PASS

        ---

        ## Validation Report: SK-006

### Summary
**PASS** — All code paths for `REPL_SUBMITTED_CODE` are verified: constant definition, depth-scoping membership, writer in `repl_tool.py`, and all four reader call-sites (`sqlite_tracing.py`, `live_loader.py`, `flow_builder.py`, `live_invocation_tree.py`) are correctly implemented.

### Evidence

1. **Constant definition** — `state.py:75`: `REPL_SUBMITTED_CODE = "repl_submitted_code"`. Value is the exact string `"repl_submitted_code"`.

2. **DEPTH_SCOPED_KEYS membership** — `state.py:151`: `REPL_SUBMITTED_CODE` is listed in the `DEPTH_SCOPED_KEYS` set, confirming depth-scoping is enabled.

3. **depth_key() function** — `state.py:166-182`: Returns bare key at depth 0, appends `@d{depth}` at depth > 0. Logic: `if depth == 0: return key` / `return f"{key}@d{depth}"`.

4. **Writer (repl_tool.py:125)** — `tool_context.state[depth_key(REPL_SUBMITTED_CODE, self._depth)] = code`. Uses the correct mutation path (`tool_context.state`, not `ctx.session.state`), with depth-scoping via `depth_key()`. Sibling keys (`_CHARS`, `_HASH`, `_PREVIEW`) also written at lines 126-128.

5. **Reader: sqlite_tracing.py:68** — `if key.startswith("repl_submitted_code"): return "repl"`. This `startswith` check correctly handles both the bare key and depth-suffixed variants like `repl_submitted_code@d1`, as well as sibling keys `repl_submitted_code_hash`, `repl_submitted_code_preview`, `repl_submitted_code_chars`.

6. **Reader: live_loader.py:1036-1083** — Builds `latest_repl` dict filtering `state_items` by `item.base_key in {REPL_SUBMITTED_CODE, REPL_EXPANDED_CODE}` (line 1039). Then at line 1079-1083, populates `LiveInvocation.repl_submission` with `str(latest_repl.get(REPL_SUBMITTED_CODE).value if latest_repl.get(REPL_SUBMITTED_CODE) else "")`. Correctly uses `base_key` (not `raw_key`), so depth-suffixed keys like `repl_submitted_code@d1` are matched after `parse_depth_key()` decomposition upstream.

7. **Reader: flow_builder.py:106** — `code = inv.repl_submission or ""`. Reads the hydrated field from `LiveInvocation` to decide whether to render a code cell (line 108: `if code.strip() or expanded.strip()`).

8. **Reader: live_invocation_tree.py:191** — `if si.base_key == "repl_submitted_code_hash" and si.depth == inv.depth`. Used in `_loop_detection_warning()` to detect repeated code submissions by comparing SHA-256 hashes across iterations. References the sibling key (string literal matches `REPL_SUBMITTED_CODE_HASH` constant value).

9. **LiveInvocation model** — `live_models.py:197`: `repl_submission: str` field exists as a frozen dataclass field. Also present in `LivePane` (line 232).

### Bugs Found
No bugs found. All paths are correctly implemented.

### Validation Notes
This validation statically verified the complete write-read lifecycle of `REPL_SUBMITTED_CODE` across 7 source files. The constant is defined at `state.py:75`, included in `DEPTH_SCOPED_KEYS` at line 151, written via the correct `tool_context.state` mutation path in `repl_tool.py:125` with depth-scoping, classified as `"repl"` category by `sqlite_tracing.py:68` using a `startswith` check that naturally handles `@dN` suffixes, hydrated into `LiveInvocation.repl_submission` by `live_loader.py:1079`, consumed by `flow_builder.py:106` for code cell rendering, and used for loop detection via the sibling `_hash` key in `live_invocation_tree.py:191`. The test plan's 10 assertions (6 Python + 4 grep) align with the evidence found. One minor note: `live_invocation_tree.py:191` uses the string literal `"repl_submitted_code_hash"` rather than importing the `REPL_SUBMITTED_CODE_HASH` constant — this is a cosmetic concern only, as the values are identical.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer's evidence is specific, line-accurate, and directly verified against the source code. The most critical claim — that the writer uses `tool_context.state[depth_key(REPL_SUBMITTED_CODE, self._depth)] = code` at `repl_tool.py:125` — is confirmed exactly as reported. This is the correct ADK mutation path (not `ctx.session.state`), satisfying the AR-CRIT-001 rule. The constant definition at `state.py:75` matches the expected string value `"repl_submitted_code"`, and `DEPTH_SCOPED_KEYS` membership at line 151 confirms depth-scoping. The reader evidence across four files includes actual code patterns and logic explanations (e.g., `startswith` handling of `@dN` suffixes, `base_key` filtering after `parse_depth_key()` decomposition), not just "the import exists." The minor observation about a string literal vs. constant import in `live_invocation_tree.py` demonstrates genuine code inspection rather than rubber-stamping.

REWARD_HACK_DETECTED: no
