        # Validation Report: LC-001 — Launch button

        **Category**: launch_controls
        **Validated**: 2026-03-23 04:12
        **Verdict**: PASS

        ---

        ## Validation Report: LC-001

### Summary
**PASS** — All four described behaviors (launch sets state, re-entrance guard, cancel lifecycle, UI branching) are fully implemented with direct code evidence.

### Evidence

1. **Launch sets `launch_in_progress=True`, `launch_cancelled=False`, assigns `launched_session_id`**
   - `live_controller.py:113`: `self.state.launch_in_progress = True`
   - `live_controller.py:114`: `self.state.launch_cancelled = False`
   - `live_controller.py:116`: `self.state.launched_session_id = handle.session_id`
   - `live_controller.py:117-118`: Prepends `handle.session_id` to `available_sessions`
   - `live_controller.py:121`: Creates `self._launch_task = asyncio.create_task(self._run_launch(handle))`
   - Return value is `handle.session_id` (line 122)

2. **Re-entrance guard (no-op when already in progress)**
   - `live_controller.py:92-93`: `if self.state.launch_in_progress: return None`
   - This is the first check in `launch_replay()`, before any side effects.

3. **No-fixture guard**
   - `live_controller.py:106-108`: `else: self.state.launch_error = "No fixture selected"; return None`
   - Reached when both `selected_provider_fake_fixture` and `replay_path` are falsy.

4. **Cancel lifecycle**
   - `live_controller.py:135-148`: `cancel_launch()` checks `_launch_task` is not None/done (line 137), calls `.cancel()` (line 139), sets `launch_in_progress=False` (line 142), `launch_cancelled=True` (line 143), and optionally marks the trace cancelled (lines 144-148).
   - Safe no-op: line 137-138 returns early if `_launch_task is None or _launch_task.done()`.

5. **`_run_launch` resets `launch_in_progress` on completion**
   - `live_controller.py:132-133`: `finally: self.state.launch_in_progress = False` — ensures flag resets whether run succeeds, fails, or is cancelled.

6. **UI branching: Cancel vs Launch button**
   - `live_app.py:421`: `if controller.state.launch_in_progress:`
   - `live_app.py:422`: `cancel_button = ui.button("Cancel", on_click=on_cancel)`
   - `live_app.py:429`: `else:` branch creates `launch_button = ui.button(launch_label, on_click=on_launch)`

7. **Launch button disabled when no fixture on-deck**
   - `live_app.py:382-386`: `on_deck_label` is set to `""` when neither `replay_path` nor `selected_provider_fake_fixture` is set.
   - `live_app.py:435-436`: `if not on_deck_label: launch_button.disable()`

8. **State model fields exist**
   - `live_models.py:328-331`: `LiveDashboardState` has `launch_in_progress: bool = False`, `launch_cancelled: bool = False`, `launch_error: str | None = None`, `launched_session_id: str | None = None`.

9. **Controller `_launch_task` initialized**
   - `live_controller.py:52`: `self._launch_task: asyncio.Task | None = None`

### Bugs Found
No bugs found. All described behaviors are implemented correctly.

### Validation Notes
All four behavioral claims in the manifest were validated against source code: (a) `launch_replay()` mutates the correct state fields and returns the session ID, (b) the re-entrance guard short-circuits before any side effects, (c) `cancel_launch()` cancels the task and sets both flags with a safe no-op path, and (d) the `_launch_panel` conditionally renders Cancel vs Launch and disables the Launch button when no fixture is selected. The `_run_launch` finally block ensures `launch_in_progress` resets even on exceptions. One caveat: this is static validation — runtime NiceGUI rendering behavior (e.g., that `ui.button.disable()` actually greys out the element) is trusted to the framework.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer's report demonstrates direct, line-numbered code evidence for every behavioral claim in the manifest. The launch button's state mutation path is traced through specific assignments (lines 113-122), the re-entrance guard is confirmed as the first check before side effects (line 92-93), the cancel lifecycle is verified with task cancellation and flag toggling (lines 135-148), and the UI branching between Cancel/Launch buttons is confirmed via the conditional at live_app.py:421-429. The evidence is structural, not nominal — it cites specific state field assignments and control flow, not just the existence of functions or files. The caveat about NiceGUI runtime rendering is appropriate and honest; trusting framework primitives like `ui.button.disable()` is reasonable for static validation.

REWARD_HACK_DETECTED: no
