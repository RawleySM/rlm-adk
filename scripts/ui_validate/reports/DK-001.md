        # Validation Report: DK-001 — MESSAGE_HISTORY

        **Category**: dead_key_candidates
        **Validated**: 2026-03-23 03:07
        **Verdict**: PASS

        ---

        All evidence gathered. Here is the report:

## Validation Report: DK-001

### Summary
**PASS** — `MESSAGE_HISTORY` is declared at `rlm_adk/state.py:20` but is never imported, read, or written by any production or test Python code, and is absent from all curated key sets.

### Evidence

1. **Sole declaration**: `MESSAGE_HISTORY = "message_history"` at `rlm_adk/state.py:20` — confirmed via grep.
2. **Zero production references**: Grep for `MESSAGE_HISTORY`, `"message_history"`, and `'message_history'` across all `rlm_adk/**/*.py` files returned exactly one hit — the declaration itself. No imports, no reads, no writes.
3. **Zero test references**: Same grep across `tests_rlm_adk/**/*.py` returned zero hits.
4. **Absent from `DEPTH_SCOPED_KEYS`** (`state.py:145-159`): Set contains `CURRENT_DEPTH`, `ITERATION_COUNT`, `FINAL_RESPONSE_TEXT`, `LAST_REPL_RESULT`, `SHOULD_STOP`, and REPL code keys — no `MESSAGE_HISTORY`.
5. **Absent from `EXPOSED_STATE_KEYS`** (`state.py:131-142`): Contains `ITERATION_COUNT`, `CURRENT_DEPTH`, `APP_MAX_ITERATIONS`, `APP_MAX_DEPTH`, `LAST_REPL_RESULT`, `STEP_MODE_ENABLED`, `SHOULD_STOP`, `FINAL_RESPONSE_TEXT` — no `MESSAGE_HISTORY`.
6. **Absent from `CURATED_STATE_KEYS`** (`state.py:205-214`): Contains `CURRENT_DEPTH`, `ITERATION_COUNT`, `SHOULD_STOP`, `FINAL_RESPONSE_TEXT`, `LAST_REPL_RESULT`, `DYN_SKILL_INSTRUCTION` — no `MESSAGE_HISTORY`.
7. **Not matched by `CURATED_STATE_PREFIXES`** (`state.py:216-227`): Prefixes are `obs:`, `artifact_`, `skill_`, `last_repl_result`, `repl_submitted_code`, `repl_expanded_code`, `repl_skill_expansion_meta`, `repl_did_expand`, `reasoning_`, `final_answer`. The string `"message_history"` does not start with any of these.
8. **Stale doc references only**: All other hits for `message_history` or `MESSAGE_HISTORY` are in `rlm_adk_docs/stale_docs_DO_NOT_READ/`, `repomix-*.xml` (compressed snapshots), `ai_docs/` (generated reference), and `scripts/ui_validate/manifest.json` (this validation manifest itself). None are executable code.

### Bugs Found
No bugs. The key is dead as declared by the manifest.

### Validation Notes
Static grep search confirmed that `MESSAGE_HISTORY` exists only as a constant declaration in `state.py:20` with zero imports or usages across all production (`rlm_adk/`) and test (`tests_rlm_adk/`) Python modules. It was also verified absent from all four curated key collections (`DEPTH_SCOPED_KEYS`, `EXPOSED_STATE_KEYS`, `CURATED_STATE_KEYS`) and unmatched by any `CURATED_STATE_PREFIXES` entry, meaning it is never captured for observability. The remaining references are exclusively in stale documentation directories and auto-generated XML snapshots that describe a prior architecture (pre-orchestrator-collapse). This key is safe to remove.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer's evidence is thorough and directly addresses the manifest claim. Rather than simply confirming the constant exists, the report demonstrates exhaustive grep coverage across both production and test Python files, finding zero imports, reads, or writes beyond the declaration itself. The additional verification against all four curated key collections (DEPTH_SCOPED_KEYS, EXPOSED_STATE_KEYS, CURATED_STATE_KEYS, CURATED_STATE_PREFIXES) rules out indirect usage through set membership. The report correctly identifies and dismisses non-code references (stale docs, XML snapshots) as irrelevant to runtime behavior. This is a clean, well-evidenced confirmation of a dead key.

REWARD_HACK_DETECTED: no
