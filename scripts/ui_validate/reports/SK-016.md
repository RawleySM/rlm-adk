        # Validation Report: SK-016 — ROOT_PROMPT

        **Category**: state_keys
        **Validated**: 2026-03-23 00:24
        **Verdict**: PASS

        ---

        ## Validation Report: SK-016

### Summary
**PASS** — The `root_prompt` state key is correctly defined, written by the orchestrator via `initial_state` / `state_delta`, resolved in the dynamic instruction template, and read by the dashboard's `LiveDashboardLoader` for user-facing display.

### Evidence

1. **Constant definition** (`rlm_adk/state.py:26,33`):
   - `ROOT_PROMPT = "root_prompt"` — context metadata constant
   - `DYN_ROOT_PROMPT = "root_prompt"` — dynamic instruction template constant
   - Both resolve to the identical string literal `"root_prompt"`, ensuring the writer key and the ADK template placeholder target the same state slot.

2. **Writer — orchestrator initial_state** (`rlm_adk/orchestrator.py:396-398`):
   - Guarded by `if self.root_prompt:`, then sets both `initial_state[ROOT_PROMPT] = self.root_prompt` and `initial_state[DYN_ROOT_PROMPT] = self.root_prompt`.
   - This writes the prompt text into session state before the reasoning agent begins, making it available for template resolution and downstream readers.

3. **Template substitution** (`rlm_adk/utils/prompts.py:111`):
   - `RLM_DYNAMIC_INSTRUCTION` contains `Original query: {root_prompt?}` — the `{root_prompt?}` placeholder is resolved by ADK's built-in state variable injection from the `"root_prompt"` session state key.

4. **Dashboard reader — LiveDashboardLoader** (`rlm_adk/dashboard/live_loader.py:263-264`):
   - `session_summary()` resolves `user_query` via: `self._latest_session_text(sse_rows, "root_prompt") or str(trace_row.get("root_prompt_preview") or "")`.
   - Falls back from SSE state events to the `root_prompt_preview` column in the traces table — two-tier resolution with graceful degradation.
   - The resulting value is assigned to `LiveSessionSummary.user_query` (line 270).

5. **SQLite tracing reader** (`rlm_adk/dashboard/live_loader.py:541`):
   - The SQL query selects `root_prompt_preview` from the `traces` table, confirming the value is persisted and queryable for dashboard display.

6. **Replay fixture** (`tests_rlm_adk/replay/recursive_ping.json`):
   - Contains `"root_prompt": "Delegate to a child LLM via execute_code + llm_query and report its answer."` in the `state` block, confirming the key is exercised in replay-driven tests.

### Bugs Found
None.

### Validation Notes
All five layers of the `root_prompt` data flow were verified through direct source inspection: constant definition, orchestrator write path (guarded, dual-key), ADK template placeholder, dashboard reader (with two-tier fallback), and fixture coverage. The evidence confirms that a prompt string set on `RLMOrchestratorAgent.root_prompt` propagates through `initial_state` into session state, is substituted into the reasoning agent's dynamic instruction via `{root_prompt?}`, and surfaces in the dashboard UI via `LiveSessionSummary.user_query`. The only caveat is that checks (b) and (d) from the test plan rely on source-text pattern matching, which could false-negative after a non-trivial refactor — but the current implementation directly matches all expected patterns.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer traced the full five-layer data flow — constant definition, orchestrator write, template placeholder, dashboard reader, and fixture coverage — with specific file paths and line numbers for each hop. Crucially, the write-path evidence goes beyond "the constant is defined": it identifies the guarded `initial_state[ROOT_PROMPT] = self.root_prompt` assignment and explains how that seeds session state before the reasoning agent starts. The reader-side evidence is similarly concrete, showing the two-tier resolution in `live_loader.py` (SSE state events → `root_prompt_preview` column fallback). One minor inaccuracy: the manifest labels the writer mechanism as "state_delta" but the actual write path is `initial_state` dict seeding, not `EventActions(state_delta={...})`; the implementer blurred these together with "initial_state / state_delta" rather than flagging the discrepancy. This is not a validation failure — `initial_state` is a legitimate ADK write path that correctly populates session state — but the imprecision should be noted.

REWARD_HACK_DETECTED: no
