        # Validation Report: SK-010 — REPL_EXPANDED_CODE

        **Category**: state_keys
        **Validated**: 2026-03-23 00:04
        **Verdict**: PASS

        ---

        Now I have all the evidence needed. Here is the validation report:

## Validation Report: SK-010

### Summary
**PASS** — `REPL_EXPANDED_CODE` is correctly defined, depth-scoped, conditionally written only when skill expansion occurs, and consumed by the full reader pipeline through to the dashboard UI.

### Evidence

1. **Constant definition** — `REPL_EXPANDED_CODE = "repl_expanded_code"` at `rlm_adk/state.py:81`.

2. **Depth-scoping** — `REPL_EXPANDED_CODE` is a member of `DEPTH_SCOPED_KEYS` at `rlm_adk/state.py:155`. `depth_key()` (line 166-182) returns the bare key at depth 0 and `key@dN` at depth N>0.

3. **Observability capture** — `"repl_expanded_code"` is listed in `CURATED_STATE_PREFIXES` at `rlm_adk/state.py:222`, so `should_capture_state_key()` returns `True` for this key via the prefix match.

4. **Conditional write in REPLTool** — `rlm_adk/tools/repl_tool.py:187-195`: The write `tool_context.state[depth_key(REPL_EXPANDED_CODE, self._depth)] = exec_code` is inside the `if expansion.did_expand:` guard (line 187). The constant is imported from `state.py` at line 36. There is exactly **one** write site for `REPL_EXPANDED_CODE` with `tool_context.state` in the entire file (line 189), and it is syntactically nested inside the guard. The write correctly uses `tool_context.state` (not `ctx.session.state`), complying with AR-CRIT-001.

5. **Reader: live_loader.py** — Imports `REPL_EXPANDED_CODE` from `rlm_adk.state` at line 39. Uses it to filter state items at line 1039 (`item.base_key in {REPL_SUBMITTED_CODE, REPL_EXPANDED_CODE}`), and reads the value at lines 1084-1087 to populate `LiveInvocation.repl_expanded_code`.

6. **Data model: LiveInvocation** — `rlm_adk/dashboard/live_models.py:198`: field `repl_expanded_code: str` exists on the `LiveInvocation` dataclass (confirmed at line 198). Also present on `LivePane` at line 233.

7. **Reader: flow_builder.py** — `rlm_adk/dashboard/flow_builder.py:107`: reads `inv.repl_expanded_code` and passes it as `expanded_code` to `FlowCodeCell` at line 138.

8. **Data model: FlowCodeCell** — `rlm_adk/dashboard/flow_models.py:66`: field `expanded_code: str = ""` exists on the frozen dataclass.

9. **Reader: flow_code_pane.py** — `rlm_adk/dashboard/components/flow_code_pane.py:81`: `code = cell.code or cell.expanded_code` uses the expanded code as a fallback when `cell.code` is empty/falsy. This is the terminal rendering point.

### Bugs Found
None.

### Validation Notes
The full write-through-read pipeline was validated statically: constant definition in `state.py` → conditional write via `tool_context.state` inside the `did_expand` guard in `repl_tool.py` → state item lookup in `live_loader.py` using the same imported constant → population of `LiveInvocation.repl_expanded_code` → pass-through in `flow_builder.py` to `FlowCodeCell.expanded_code` → fallback rendering in `flow_code_pane.py`. The depth-scoping (`depth_key()`) is applied at the write site, and the reader (`live_loader.py`) uses `item.base_key` for lookup, which is the parsed base key stripped of the `@dN` suffix — confirming depth-scoped reads work correctly. The only limitation is that no replay fixture was statically confirmed to trigger `did_expand=True`, so runtime proof of the conditional branch executing would require a targeted unit test or a fixture with synthetic skill imports.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's report demonstrates thorough static validation of the full write-through-read pipeline for `REPL_EXPANDED_CODE`. The critical AR-CRIT-001 check was performed correctly — the report confirms the write uses `tool_context.state` (not `ctx.session.state`) and cites the specific line number (189) where this occurs. The conditional guard (`if expansion.did_expand:`) was verified as the enclosing scope of the write, matching the manifest's "only written when expansion.did_expand is True" requirement. The depth-scoping was validated at both ends: `depth_key()` applied at the write site and `item.base_key` used for lookup in `live_loader.py`, confirming the `@dN` suffix round-trips correctly. The reader pipeline was traced through four files with specific line citations. The one honest limitation noted — no runtime proof that `did_expand=True` fires in an existing fixture — is a fair caveat but does not undermine the structural correctness of the validation.

REWARD_HACK_DETECTED: no
