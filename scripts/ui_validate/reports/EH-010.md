        # Validation Report: EH-010 — _open_text_viewer / _open_invocation_context_viewer

        **Category**: event_handlers
        **Validated**: 2026-03-23 06:51
        **Verdict**: PASS

        ---

        I now have complete evidence. Here is the validation report:

## Validation Report: EH-010

### Summary
**PASS** — All three controller methods (`open_context_viewer`, `open_invocation_context_viewer`, `open_text_viewer`) correctly populate `state.context_selection` as a `LiveContextSelection` with all 5 fields and set `state.context_viewer_open = True`. The thin wrappers in `live_app.py` delegate to the controller and call `refresh_viewer.refresh()`.

### Evidence

1. **`open_context_viewer(item)` — `live_controller.py:255-268`**
   - Calls `self.loader.resolve_banner_item_text(self.selected_invocation(self.state.active_pane), item, lineage=self.selected_invocation_lineage())` to resolve text
   - Constructs `LiveContextSelection(label=item.label, raw_key=item.raw_key, scope=item.scope, source_kind=item.source_kind, text=text)`
   - Sets `self.state.context_viewer_open = True`

2. **`open_invocation_context_viewer(invocation, item, lineage)` — `live_controller.py:270-288`**
   - Calls `self.loader.resolve_banner_item_text(invocation, item, lineage=lineage)` with explicitly passed invocation/lineage (not self-resolved)
   - Constructs identical `LiveContextSelection` with all 5 fields from `item`
   - Sets `self.state.context_viewer_open = True`

3. **`open_text_viewer(*, label, text, raw_key)` — `live_controller.py:307-316`**
   - Strips whitespace: `normalized = text.strip()`
   - Falls back on empty: `text=normalized or f"No {label} captured."`
   - Scope hardcoded to `"tool_variable"`, source_kind to `"tool_variable"`
   - Sets `self.state.context_viewer_open = True`

4. **`LiveContextSelection` — `live_models.py:90-96`**
   - `@dataclass(frozen=True)` with fields: `label: str`, `raw_key: str`, `scope: str`, `source_kind: SourceKind`, `text: str`
   - `SourceKind` is `Literal["dynamic_instruction_param", "state_key", "request_chunk", "tool_variable"]` (line 8-13)

5. **`LiveDashboardState.context_viewer_open` — `live_models.py:341`**
   - `context_viewer_open: bool = False` — defaults to `False`, set to `True` by all three methods

6. **`LiveContextBannerItem` — `live_models.py:79-87`**
   - Has matching fields: `label`, `raw_key`, `scope`, `source_kind` — these are passed through to `LiveContextSelection`

7. **Thin wrapper `_open_invocation_context_viewer` — `live_app.py:672-680`**
   - Calls `controller.open_invocation_context_viewer(invocation, item, lineage)` then `refresh_viewer.refresh()`

8. **Thin wrapper `_open_text_viewer` — `live_app.py:698-706`**
   - Calls `controller.open_text_viewer(label=label, text=text, raw_key=raw_key)` then `refresh_viewer.refresh()`

9. **Trigger sites** — `live_app.py:279` passes `_open_invocation_context_viewer` as `on_open_context` lambda; `live_app.py:609` binds `_open_text_viewer` to `click.stop` event on state chips

### Bugs Found

**No bugs found.** The test plan's Test 1 will require adjustment though: `open_context_viewer` calls `self.selected_invocation(self.state.active_pane)` (not `self.selected_invocation()` without args), and `self.state.active_pane` is a property that requires `self.state.snapshot` to be populated with panes. The test plan mocks `ctrl.selected_invocation` as a MagicMock, which will intercept the call correctly, but note that `open_context_viewer` passes `self.state.active_pane` (which will be `None` without a snapshot), not `self.state.active_pane_id`. This is fine for the mock-based test but worth noting.

One minor test plan issue (not a code bug):
- **Test Plan Note:** Test 1 patches `ctrl.selected_invocation` as a `MagicMock`, but the real method signature is `selected_invocation(self, pane)`. The mock will accept any args, so it works, but `open_context_viewer` passes `self.state.active_pane` (which is `None` without a snapshot). The mock intercepts before any `None` propagation, so the test is valid.

### Validation Notes

All three context viewer methods follow the same pattern: resolve text content, construct a `LiveContextSelection` with the 5 required fields, and flip `context_viewer_open = True`. The `open_text_viewer` and `open_repl_output_viewer` methods add whitespace stripping and an empty-text fallback (`"No {label} captured."`). The thin wrappers in `live_app.py` are pure delegation + refresh — no logic of their own. The trigger sites at `live_app.py:279` and `live_app.py:609` confirm these handlers are wired to UI click events. Static validation is high-confidence here because the implementation is straightforward dataclass construction with no async or conditional branching beyond the text normalization.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer traced each handler end-to-end: from the UI click binding sites (live_app.py:279, :609) through the thin wrappers to the controller methods, verifying that each constructs a `LiveContextSelection` with all 5 fields and flips `context_viewer_open = True`. The evidence is direct — specific line numbers, actual field names matched against the frozen dataclass definition, and the text-resolution call chains are described, not just asserted. The whitespace-stripping and empty-text fallback in `open_text_viewer` show the reviewer read the implementation logic, not just the function signature. The honest note about `active_pane` being `None` without a snapshot (and why the mock-based test still works) further demonstrates genuine code reading rather than surface-level pattern matching.

REWARD_HACK_DETECTED: no
