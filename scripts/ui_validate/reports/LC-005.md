        # Validation Report: LC-005 — Prompt-visible skills multi-select

        **Category**: launch_controls
        **Validated**: 2026-03-23 04:30
        **Verdict**: PASS

        ---

        ## Validation Report: LC-005

### Summary
**PASS** — The prompt-visible skills multi-select control is fully implemented with correct UI wiring, normalization logic, state management, downstream propagation, and pill display.

### Evidence

1. **UI widget (`ui.select` with `multiple=True`)**
   - `live_app.py:489-496`: `ui.select(options=skill_options, value=controller.state.selected_skills, label="Prompt-visible skills", multiple=True, ...)`
   - `on_change` lambda: `lambda e: controller.set_selected_skills(list(e.value or []))` — correctly coerces `None` to empty list.

2. **Skill options population**
   - `live_app.py:367`: `skill_options = [name for name, _ in selected_skill_summaries(None)]` — passing `None` returns all registry skill names (via `normalize_enabled_skill_names(None) → DEFAULT_ENABLED_SKILL_NAMES`), so the dropdown options contain every registered skill.

3. **Handler — `set_selected_skills()`**
   - `live_controller.py:87-89`: Calls `normalize_enabled_skill_names(selected_skills)`, stores result as `list(...)` into `self.state.selected_skills`, then clears `self.state.launch_error = None`.

4. **Normalization logic — `normalize_enabled_skill_names()`**
   - `catalog.py:148-158`: Returns `DEFAULT_ENABLED_SKILL_NAMES` for `None` input. For non-None, builds a set of requested names, raises `ValueError` for unknown names (`sorted(requested - PROMPT_SKILL_REGISTRY.keys())`), then filters `DEFAULT_ENABLED_SKILL_NAMES` to requested names — preserving registry insertion order.

5. **State field**
   - `live_models.py:327`: `selected_skills: list[str] = field(default_factory=list)` — dataclass field with empty-list default.

6. **Controller init default**
   - `live_controller.py:51`: `self.state.selected_skills = list(DEFAULT_ENABLED_SKILL_NAMES)` — all 8 registry keys selected on init.

7. **`DEFAULT_ENABLED_SKILL_NAMES`**
   - `catalog.py:131`: `DEFAULT_ENABLED_SKILL_NAMES: tuple[str, ...] = tuple(PROMPT_SKILL_REGISTRY.keys())` — tuple of all 8 registry keys.

8. **`PROMPT_SKILL_REGISTRY`**
   - `catalog.py:88-129`: Contains 8 entries: repomix, polya_understand, polya_narrative, polya_understand_t1_workflow, polya_understand_t2_flat, polya_understand_t3_adaptive, polya_understand_t4_debate, ping.

9. **Pill display**
   - `live_app.py:504-513`: Iterates `selected_skill_summaries(controller.state.selected_skills)`, renders each `(name, description)` as a rounded pill div with `ui.label(name)` and `.tooltip(description)`.

10. **`selected_skill_summaries()`**
    - `catalog.py:169-179`: Returns `[(reg.name, reg.description)]` for each normalized skill name — correct `(name, description)` tuples.

11. **Downstream propagation**
    - `live_controller.py:99`: `prepare_provider_fake_launch(..., enabled_skills=self.state.selected_skills)`
    - `live_controller.py:104`: `prepare_replay_launch(..., enabled_skills=self.state.selected_skills)`
    - Both launch paths pass the selected skills to the runner.

12. **Import chain**
    - `live_controller.py:28`: `from rlm_adk.skills import DEFAULT_ENABLED_SKILL_NAMES, normalize_enabled_skill_names`
    - `live_app.py:15`: `from rlm_adk.skills import selected_skill_summaries`
    - `skills/__init__.py` re-exports all catalog functions.

### Bugs Found
None.

### Validation Notes
All seven aspects of the multi-select control were validated through static code analysis: (1) the `ui.select` widget renders with `multiple=True` and all registry skills as options, (2) the `on_change` handler calls `set_selected_skills()` which normalizes via registry-order filtering and rejects unknown names with `ValueError`, (3) the state field exists with correct default, (4) controller init populates it with all defaults, (5) pill display iterates `selected_skill_summaries()` for tooltip rendering, (6) both launch paths propagate `selected_skills` as the `enabled_skills` kwarg, and (7) the import chain from `catalog.py` through `__init__.py` to both consumer files is intact. The only limitation is that NiceGUI's runtime reactivity (whether pill display re-renders on selection change) cannot be verified statically — it depends on NiceGUI's server-side rendering lifecycle.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer's report demonstrates thorough static analysis across the full data flow: from the `ui.select` widget configuration with `multiple=True` (live_app.py:489-496), through the handler (`set_selected_skills` at live_controller.py:87-89), normalization logic (`normalize_enabled_skill_names` at catalog.py:148-158 with unknown-name rejection), state storage (live_models.py:327), default initialization (live_controller.py:51), pill display rendering (live_app.py:504-513), and downstream propagation to both launch paths (live_controller.py:99,104). The evidence is specific with line numbers and includes actual code signatures rather than just naming conventions. The report correctly acknowledges the limitation that NiceGUI runtime reactivity cannot be verified statically, which is an honest assessment rather than a gap. The normalization function's error-handling path (raising `ValueError` for unknown skill names) and the order-preserving filtering logic were both verified, which goes beyond surface-level checks.

REWARD_HACK_DETECTED: no
