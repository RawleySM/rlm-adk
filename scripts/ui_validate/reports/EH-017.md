        # Validation Report: EH-017 — on_click_llm_query_line

        **Category**: event_handlers
        **Validated**: 2026-03-23 07:25
        **Verdict**: PASS

        ---

        ## Validation Report: EH-017

### Summary
**PASS** (with caveat) — The `on_click_llm_query_line` handler plumbing is fully implemented end-to-end: `render_flow_transcript` → `_render_block` → `render_flow_code_pane` → `_render_code_line` correctly threads the callback and attaches `el.on("click.stop")` with the triple-guard. However, the handler is **not wired at either production call site** (live_app.py:778, flow_child_page.py:93), so clicking an llm_query line currently has no effect in the running dashboard.

### Evidence

1. **Callback parameter declaration** — `flow_transcript.py:50`: `on_click_llm_query_line=None` in `render_flow_transcript` signature.

2. **Threading hop 1** — `flow_transcript.py:69`: `on_click_llm_query_line=on_click_llm_query_line` passed to `_render_block()`.

3. **Threading hop 2** — `flow_transcript.py:98`: `on_click_llm_query_line=on_click_llm_query_line` passed to `render_flow_code_pane()`.

4. **Acceptance in code pane** — `flow_code_pane.py:78`: `on_click_llm_query_line=None` accepted; line 120: forwarded as `on_click=on_click_llm_query_line` to `_render_code_line()`.

5. **Triple-guard** — `flow_code_pane.py:142`: `if is_llm_query and on_click and llm_info:` — all three conditions must be truthy before attaching the click listener.

6. **Click listener attachment** — `flow_code_pane.py:143`: `el.on("click.stop", lambda _e, info=llm_info: on_click(info))` — delivers `LlmQueryLineInfo` to the callback.

7. **Cursor styling** — `flow_code_pane.py:135`: `cursor = "pointer" if is_llm_query and on_click else "default"` — visual affordance only when handler is supplied.

8. **`find_llm_query_lines` AST detection** — `flow_builder.py:24-55`: parses code via `ast.parse`, walks for `ast.Call` nodes matching `{"llm_query", "llm_query_batched"}`, extracts `output_schema` keyword, returns sorted `list[tuple[int, str, str | None]]`. SyntaxError → `[]`.

9. **`LlmQueryLineInfo` dataclass** — `flow_models.py:46-57`: frozen dataclass with all 9 fields: `line_number`, `function_name`, `schema_name`, `child_depth`, `child_fanout_idx`, `child_status`, `child_prompt_preview`, `child_result_preview`, `child_pane_id`.

10. **`LlmQueryLineInfo` population** — `flow_builder.py:116-132`: `_process_node` matches llm_query lines to `child_summaries` by source order, populates all fields including `child_pane_id` via `_find_child_pane_id()`.

11. **NOT wired in live_app.py** — `live_app.py:778-790`: `render_flow_transcript` called with `on_select_iteration`, `on_open_context`, `on_open_child_window` but **omits** `on_click_llm_query_line`.

12. **NOT wired in flow_child_page.py** — `flow_child_page.py:93`: `render_flow_transcript(transcript)` called with zero keyword arguments.

### Bugs Found

- **Bug**: `on_click_llm_query_line` is not wired at either call site, so the "Navigates to child pane" behavior described in the manifest never executes in production.
- **Location**: `live_app.py:778` and `flow_child_page.py:93`
- **Severity**: warning (feature is implemented but inert — no runtime error, just dead code path)
- **Fix**: At `live_app.py:778`, add `on_click_llm_query_line=lambda info: _open_child_window(controller, info)` (or equivalent navigation callback). At `flow_child_page.py:93`, thread the same pattern to enable click-to-navigate in child transcript views as well. The `LlmQueryLineInfo.child_pane_id` field already carries the target pane, so the callback implementation should use `controller.navigate_to_pane(info.child_pane_id)` or similar.

### Validation Notes

The handler's full plumbing chain (4 hops from `render_flow_transcript` to `el.on("click.stop")`) is correctly implemented with proper guard conditions and callback delivery. The `find_llm_query_lines` function uses robust AST parsing (not regex), handles both `llm_query` and `llm_query_batched`, and extracts `output_schema` names. The `LlmQueryLineInfo` frozen dataclass has all 9 expected fields and is correctly populated from child summary data in `flow_builder._process_node`. The only gap is that neither production call site passes a callback, making the entire click-to-navigate feature inert. This is likely intentional incomplete work (the plumbing was laid first, wiring deferred), not a bug per se, but it means the manifest's described behavior ("Navigates to child pane") does not occur in the running dashboard.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's report is thorough and honest. It traces the full callback plumbing chain across 4 hops with specific file paths and line numbers, verifies the triple-guard logic, confirms AST-based detection (not regex), and validates the `LlmQueryLineInfo` dataclass structure. Critically, the report explicitly flags that the handler is **not wired at either production call site**, meaning the manifest's "Navigates to child pane" behavior never fires — and correctly identifies this as a bug rather than hiding it. The severity rating of "warning" is arguably generous (this is a feature that simply doesn't work from the user's perspective), but the report is transparent about the gap and provides a concrete fix path. The evidence is direct, not circumstantial — line numbers, guard conditions, and call signatures are cited.

REWARD_HACK_DETECTED: no
