        # Validation Report: SK-007 — REPL_SUBMITTED_CODE_PREVIEW

        **Category**: state_keys
        **Validated**: 2026-03-22 23:54
        **Verdict**: PASS

        ---

        Now I have all the evidence I need. Let me also confirm the call flow at line 609-614 to verify depth-scoped keys are parsed before categorization:

The key evidence chain is clear from the three files. Here is the report:

## Validation Report: SK-007

### Summary
**PASS** — `REPL_SUBMITTED_CODE_PREVIEW` is correctly defined, depth-scoped, written with `code[:500]` truncation via `tool_context.state`, and categorized as `"repl"` by the SQLite tracing plugin.

### Evidence

1. **Constant definition** (`rlm_adk/state.py:76`):
   `REPL_SUBMITTED_CODE_PREVIEW = "repl_submitted_code_preview"` — value matches manifest.

2. **Depth-scoping membership** (`rlm_adk/state.py:152`):
   `REPL_SUBMITTED_CODE_PREVIEW` is a member of `DEPTH_SCOPED_KEYS` set — confirmed at line 152.

3. **`depth_key()` function** (`rlm_adk/state.py:166-182`):
   Returns bare key at depth 0, appends `@dN` at depth > 0. Confirmed: `depth_key("repl_submitted_code_preview", 0)` → `"repl_submitted_code_preview"`, `depth_key("repl_submitted_code_preview", 2)` → `"repl_submitted_code_preview@d2"`.

4. **Write site** (`rlm_adk/tools/repl_tool.py:128`):
   `tool_context.state[depth_key(REPL_SUBMITTED_CODE_PREVIEW, self._depth)] = code[:500]`
   — Uses the imported constant (line 43), writes via `tool_context.state` (correct mutation path per AR-CRIT-001), applies `code[:500]` truncation, and wraps with `depth_key()` for depth-scoping.

5. **Capture eligibility** (`rlm_adk/state.py:221,230-234`):
   `CURATED_STATE_PREFIXES` contains `"repl_submitted_code"` (line 221). `should_capture_state_key("repl_submitted_code_preview")` returns `True` because `"repl_submitted_code_preview".startswith("repl_submitted_code")` is `True`.

6. **SQLite categorization** (`rlm_adk/plugins/sqlite_tracing.py:68-69`):
   `_categorize_key` checks `key.startswith("repl_submitted_code")` and returns `"repl"`. This function receives the **base_key** (already parsed by `_parse_key` at line 611), so both `"repl_submitted_code_preview"` and depth-scoped variants (parsed to base key before categorization) correctly categorize as `"repl"`.

7. **Parse-before-categorize flow** (`rlm_adk/plugins/sqlite_tracing.py:611-614`):
   `base_key, depth, fanout = _parse_key(raw_key)` → `category = _categorize_key(base_key)` — confirms depth suffix is stripped before categorization, so `"repl_submitted_code_preview@d1"` → base_key `"repl_submitted_code_preview"` → category `"repl"`.

### Bugs Found
None.

### Validation Notes
All three contracts specified in the test plan were validated via static source analysis: (1) the constant is defined with the correct value and is a member of `DEPTH_SCOPED_KEYS`, (2) the write site at `repl_tool.py:128` applies `code[:500]` truncation and writes via `tool_context.state` with proper depth-scoping, and (3) the SQLite tracing plugin's `_categorize_key` function classifies both bare and depth-scoped forms as category `"repl"` because `_parse_key` strips the `@dN` suffix before categorization. The only caveat is that truncation boundary behavior (code exactly 500 chars vs. >500 chars) cannot be proven by source inspection alone — `code[:500]` is Python's standard slice semantics, which preserves strings ≤500 chars and truncates longer ones, but a runtime test would provide stronger confidence.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's evidence chain is substantive and directly addresses the critical validation concerns for a state_keys item. Most importantly, evidence point #4 confirms the actual write path at `repl_tool.py:128` uses `tool_context.state` (not `ctx.session.state`), which is the AR-CRIT-001 compliance check that distinguishes real validation from superficial name-matching. The depth-scoping verification goes beyond "the constant is in the set" — it traces the full flow from `depth_key()` wrapping at the write site through `_parse_key` stripping the suffix before `_categorize_key` in the SQLite tracing plugin. One minor gap: the manifest lists `live_loader.py` and `dashboard` as readers, and the report does not verify the read path exists, but the write-side and categorization evidence is thorough enough to constitute a meaningful validation of this state key's contract. The report is also intellectually honest about the truncation boundary limitation, which strengthens rather than weakens confidence.

REWARD_HACK_DETECTED: no
