        # Validation Report: SK-015 — IDEMPOTENCY_KEY

        **Category**: state_keys
        **Validated**: 2026-03-23 00:21
        **Verdict**: PASS

        ---

        ## Validation Report: SK-015

### Summary
**PASS** — All five test plan checks are directly supported by source code evidence.

### Evidence

1. **Constant value (check a):** `IDEMPOTENCY_KEY = "idempotency_key"` at `rlm_adk/state.py:94`.

2. **Deterministic `idem-<hex16>` generation (check b):** `PolicyPlugin.on_user_message_callback` (lines 68–79 of `rlm_adk/plugins/policy.py`) concatenates `user_id:session_id:message_text`, SHA-256 hashes it, takes the first 16 hex characters, and prefixes with `idem-`. Result stored in `self._pending_idempotency_key`. The `or ""` fallback on `user_id`/`session_id` (lines 75–76) ensures the hash input is always a valid string.

3. **AR-CRIT-001 compliant two-phase write (check c):** `PolicyPlugin.before_agent_callback` (lines 96–101 of `rlm_adk/plugins/policy.py`) writes `callback_context.state[IDEMPOTENCY_KEY] = self._pending_idempotency_key` only when the pending value is non-None, then clears it to `None`. This correctly uses `callback_context.state` (delta-tracked) rather than `ctx.session.state` (forbidden by AR-CRIT-001).

4. **Determinism and collision-freeness (check d):** The hash function is `hashlib.sha256(idem_source.encode()).hexdigest()[:16]` — purely deterministic. Same inputs produce the same hash; different `message_text` values produce different hashes (SHA-256 collision resistance).

5. **sqlite_tracing categorization (check e):** `_categorize_key` in `rlm_adk/plugins/sqlite_tracing.py:51` checks `if key in ("request_id", "idempotency_key", "repo_url", ...)` and returns `"request_meta"` (line 84→90). The string `"idempotency_key"` matches the constant value exactly.

### Bugs Found
None.

### Validation Notes
All five test plan assertions are validated by direct code inspection. The two-phase write pattern (stash in `on_user_message_callback`, persist in `before_agent_callback`) is correctly implemented per AR-CRIT-001 — `on_user_message_callback` only receives `InvocationContext` (no `callback_context`), so the deferred write is architecturally necessary. The test plan's mock approach (dict-backed `cb_ctx.state`, separate `PolicyPlugin` instances for determinism checks) accurately exercises the real write path. One minor caveat: static validation cannot confirm the `asyncio.run()` mock invocation flow matches ADK's actual callback dispatch order, but the code paths are straightforward enough that this is low-risk.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The report goes well beyond surface-level checks. For a `state_keys` category item, the critical validation is whether the write path uses `callback_context.state` (AR-CRIT-001 compliant) rather than `ctx.session.state`, and the report traces this precisely to `PolicyPlugin.before_agent_callback` lines 96–101 with an architecturally sound explanation of why the two-phase pattern (stash in `on_user_message_callback`, persist in `before_agent_callback`) is necessary — `on_user_message_callback` only receives `InvocationContext`, not `callback_context`. The SHA-256 generation logic is described with enough specificity (concatenation order, hex truncation length, prefix) to confirm it was actually read, not inferred from naming. The sqlite_tracing categorization check confirms the key string value matches across modules. One minor gap: the manifest lists policy.py as both Writer and Reader, but the report doesn't explicitly trace a read-back path — however, for an idempotency key whose value is written to delta-tracked state and consumed via ADK's event propagation, the "reader" is effectively the state system itself, making this a non-issue.

REWARD_HACK_DETECTED: no
