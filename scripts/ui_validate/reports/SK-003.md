        # Validation Report: SK-003 — SHOULD_STOP

        **Category**: state_keys
        **Validated**: 2026-03-22 23:37
        **Verdict**: PASS

        ---

        Now I have all the evidence needed.

## Validation Report: SK-003

### Summary
**PASS** — All six test assertions are statically confirmed: the `SHOULD_STOP` constant equals `"should_stop"`, belongs to all three required sets, is depth-scoped correctly, is written exclusively via `depth_key()` inside `EventActions(state_delta=...)` in the orchestrator (three codepaths), and is listed in `_KNOWN_COMPLETION_KEYS` in the dashboard's `live_loader.py`.

### Evidence

1. **Constant value**: `SHOULD_STOP = "should_stop"` at `state.py:16`.

2. **Set memberships**:
   - `EXPOSED_STATE_KEYS`: `SHOULD_STOP` present at `state.py:139` (inside frozenset definition lines 131–142).
   - `DEPTH_SCOPED_KEYS`: `SHOULD_STOP` present at `state.py:150` (inside set definition lines 145–159).
   - `CURATED_STATE_KEYS`: `SHOULD_STOP` present at `state.py:209` (inside frozenset definition lines 205–214).

3. **Depth scoping logic**: `depth_key()` at `state.py:166–182` returns `key` unchanged at depth 0, and `f"{key}@d{depth}"` at depth > 0. For `SHOULD_STOP`: depth 0 → `"should_stop"`, depth 1 → `"should_stop@d1"`, depth 2 → `"should_stop@d2"`.

4. **`parse_depth_key` round-trip**: `state.py:192–200` uses regex `^(.+)@d(\d+)(?:f(\d+))?$`. Input `"should_stop@d3"` → `("should_stop", 3, None)`. Input `"should_stop"` (no match) → `("should_stop", 0, None)`.

5. **Writer correctness** (orchestrator.py — three codepaths, all via `EventActions(state_delta=...)`):
   - **Line 545**: `depth_key(SHOULD_STOP, self.depth): True` — error/retry-exhaustion path (inside `except` block, lines 539–548).
   - **Line 617**: `depth_key(SHOULD_STOP, self.depth): True` — successful completion path (lines 608–620).
   - **Line 643**: `depth_key(SHOULD_STOP, self.depth): True` — no-result/exhausted path (lines 634–646).
   - **Negative check**: No occurrence of `ctx.session.state[...SHOULD_STOP...]` or `ctx.session.state["should_stop"]` exists anywhere in `orchestrator.py`. The constant is imported at line 49 and used exclusively through `depth_key()` inside `state_delta` dicts.

6. **Dashboard reader**: `live_loader.py:76–80` defines `_KNOWN_COMPLETION_KEYS` containing `"should_stop"` at line 78. This list is iterated at line 428 in the completion-plane banner builder.

### Bugs Found
None.

### Validation Notes
All six checks from the test plan are confirmed via static code analysis. The `SHOULD_STOP` key is correctly defined, categorized in all three state key sets (`EXPOSED`, `DEPTH_SCOPED`, `CURATED`), depth-scoped via `depth_key()`, written exclusively through compliant `EventActions(state_delta=...)` paths (never via forbidden `ctx.session.state` mutation), and consumed by the dashboard's completion-plane logic. The only limitation of this static validation is that it cannot confirm runtime behavior (e.g., that `self.depth` has the expected value at each codepath), but the code structure leaves no ambiguity about the write mechanism.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence is direct and substantive across all critical dimensions. The implementer traced three concrete write sites in orchestrator.py, confirmed each uses `EventActions(state_delta={depth_key(SHOULD_STOP, self.depth): True})` — a compliant mutation path per AR-CRIT-001 — and performed a negative check confirming no forbidden `ctx.session.state` writes exist. Set memberships, depth-scoping logic, and the dashboard reader are all verified with specific line numbers. One notable discrepancy: the manifest claims the writer is `repl_tool.py (tool_context.state)`, but the implementer found the actual writer is `orchestrator.py` via `EventActions(state_delta=...)`. The implementer validated the real code correctly but should have flagged this manifest-vs-code disagreement rather than silently accepting it. This is a documentation staleness issue, not a validation failure — the actual write mechanism is AR-CRIT-001 compliant regardless of which file contains it.

REWARD_HACK_DETECTED: no
