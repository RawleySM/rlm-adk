        # Validation Report: SK-020 — USER_PROVIDED_CTX

        **Category**: state_keys
        **Validated**: 2026-03-23 00:40
        **Verdict**: PASS

        ---

        ## Validation Report: SK-020

### Summary
**PASS** — The `USER_PROVIDED_CTX` state key is correctly defined in `state.py`, written exclusively via `initial_state[]` (compliant with AR-CRIT-001), emitted through `EventActions(state_delta=initial_state)`, and injected into REPL globals in both Path A and Path B. All 5 related constants are imported and used in the orchestrator. An 8-test suite anchors the simulation to real source code.

### Evidence

1. **Constant definition** — `state.py:37`: `USER_PROVIDED_CTX = "user_provided_ctx"`. All 5 related keys defined at lines 37–43 (`USER_PROVIDED_CTX`, `USER_PROVIDED_CTX_EXCEEDED`, `USR_PROVIDED_FILES_SERIALIZED`, `USR_PROVIDED_FILES_UNSERIALIZED`, `DYN_USER_CTX_MANIFEST`).

2. **Orchestrator imports** — `orchestrator.py:34-55`: All 5 user-context constants imported from `rlm_adk.state` (lines 40, 50, 51, 52, 53).

3. **Path A write (env var)** — `orchestrator.py:436`: `initial_state[USER_PROVIDED_CTX] = uctx.ctx`. All 5 keys populated at lines 436–440. REPL globals injection at line 442: `repl.globals["user_ctx"] = uctx.ctx`.

4. **Path B write (pre-seeded session state)** — `orchestrator.py:452`: `initial_state[USER_PROVIDED_CTX] = _pre_seeded`. All 5 keys populated at lines 452–484. REPL globals injection at line 486: `repl.globals["user_ctx"] = _pre_seeded`.

5. **Path B guard read** — `orchestrator.py:450`: `ctx.session.state.get(USER_PROVIDED_CTX)` — read-only guard, not a write.

6. **AR-CRIT-001 compliance** — Grep for `ctx.session.state[USER_PROVIDED_CTX] =` (assignment) returned **zero matches**. All mutations go through `initial_state[]` dict, which is emitted via `EventActions(state_delta=initial_state)` at line 496.

7. **State delta emission** — `orchestrator.py:496`: `actions=EventActions(state_delta=initial_state)` — the `initial_state` dict (containing all user-context keys) is emitted as an ADK event, ensuring proper event tracking.

8. **dispatch.py reader** — Grep for `USER_PROVIDED_CTX` in `dispatch.py` returned **zero matches**. The manifest describes dispatch.py as a reader, but it does not directly reference this constant. The downstream consumption path is via REPL globals (`user_ctx`), not direct state reads — consistent with the RLM architecture where context goes into REPL globals, not the model prompt.

9. **Test suite** — `test_orchestrator_user_ctx.py`: 8 tests covering:
   - Tests 1–3: Env var presence/absence/invalid-dir guard logic
   - Test 4: All 5 state keys populated
   - Test 5: `repl_globals["user_ctx"] is initial_state[USER_PROVIDED_CTX]` (identity check)
   - Test 6: Max-chars eviction (`exceeded=True`, serialized vs unserialized partitioning)
   - Test 7: `inspect.getsource()` verification that real orchestrator source contains `RLM_USER_CTX_DIR`, `load_user_context`, `user_ctx`
   - Test 8: `hasattr()` verification that all 5 constants are importable from the orchestrator module

### Bugs Found

No bugs found. The manifest's claim that `dispatch.py` is a reader is inaccurate (zero references found), but this is a manifest documentation issue, not a code bug. The actual downstream consumer is the REPL globals dict, which is the correct architecture.

### Validation Notes

Static analysis confirms the full lifecycle of `USER_PROVIDED_CTX`: constant definition in `state.py`, import into `orchestrator.py`, conditional population via two mutually exclusive paths (env-var and pre-seeded session state), emission through ADK's `EventActions(state_delta=...)` mechanism (AR-CRIT-001 compliant), and injection into REPL globals for code execution access. The test suite provides strong anchoring via `inspect.getsource()` (test 7) and `hasattr()` (test 8) checks against the real orchestrator class, mitigating simulation-drift risk. The one caveat is that dispatch.py is listed as a reader in the manifest but contains no direct references to this key — the actual consumption is mediated through `repl.globals["user_ctx"]`, which is architecturally correct for the RLM pattern.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence chain is convincing across the full lifecycle of the state key. The critical validation is not just that the constant exists, but that the write path is AR-CRIT-001 compliant: items 3-4 show writes go through `initial_state[]` dict, item 6 provides a negative grep confirming zero direct `ctx.session.state[...] =` assignments, and item 7 confirms the dict is emitted via `EventActions(state_delta=initial_state)` at a specific line number. This is a substantive mutation-path audit, not a naming check. The dispatch.py discrepancy was honestly reported as a manifest documentation issue rather than papered over, and the explanation (consumption via `repl.globals["user_ctx"]` rather than direct state reads) is architecturally consistent with the RLM pattern documented in MEMORY.md. Tests 4-5 validate actual state population and object identity, providing behavioral anchoring beyond existence checks.

REWARD_HACK_DETECTED: no
