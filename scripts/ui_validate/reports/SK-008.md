        # Validation Report: SK-008 — REPL_SUBMITTED_CODE_HASH

        **Category**: state_keys
        **Validated**: 2026-03-22 23:57
        **Verdict**: PASS

        ---

        I now have all the evidence needed. Here is the validation report:

## Validation Report: SK-008

### Summary
**PASS** — `REPL_SUBMITTED_CODE_HASH` is fully implemented: defined in `state.py`, written exclusively via `tool_context.state` with `depth_key()` scoping in `repl_tool.py`, included in `DEPTH_SCOPED_KEYS`, and consumed by the dashboard's `live_invocation_tree.py` for loop detection.

### Evidence

1. **Constant definition** — `state.py:77`: `REPL_SUBMITTED_CODE_HASH = "repl_submitted_code_hash"`. Value matches the manifest's declared state key.

2. **Depth-scoped membership** — `state.py:153`: `REPL_SUBMITTED_CODE_HASH` is a member of `DEPTH_SCOPED_KEYS` set (lines 145–159).

3. **`depth_key()` function** — `state.py:166–181`: Returns bare key at depth 0, appends `@dN` suffix at depth > 0. `parse_depth_key()` at `state.py:192–200` is the inverse, returning `(base_key, depth, fanout_or_None)`.

4. **Writer (AR-CRIT-001 compliant)** — `repl_tool.py:124–127`:
   - Line 124: `code_hash = hashlib.sha256(code.encode()).hexdigest()` — computes SHA-256 hash
   - Line 127: `tool_context.state[depth_key(REPL_SUBMITTED_CODE_HASH, self._depth)] = code_hash` — writes via `tool_context.state` (correct mutation path), using `depth_key()` with `self._depth` for depth-scoping
   - `self._depth` is set at `repl_tool.py:89` from constructor parameter `depth: int = 0`

5. **No `session.state` violation** — Grep for `session\.state` in `repl_tool.py` returned **zero matches**. The key is never written via `ctx.session.state`.

6. **Reader (dashboard)** — `live_invocation_tree.py:191`: `if si.base_key == "repl_submitted_code_hash" and si.depth == inv.depth:` — used in `_loop_detection_warning()` (line 184) to detect repeated code submissions by comparing SHA-256 hashes across consecutive iterations.

7. **Reader (generic pipeline)** — `live_loader.py:957–984`: `_latest_state_by_depth()` constructs `LiveStateItem` objects from all `session_state_events` rows, keyed by `(depth, base_key)`. No key-specific filter excludes `repl_submitted_code_hash` — it flows through the generic pipeline.

8. **Import chain** — `repl_tool.py:42` imports `REPL_SUBMITTED_CODE_HASH` from `rlm_adk.state`, confirming it uses the constant (not a hardcoded string literal) for the write.

### Bugs Found
None.

### Validation Notes
All seven test dimensions from the test plan are satisfied by direct code evidence: constant definition, depth-scoped set membership, `depth_key()`/`parse_depth_key()` round-tripping, writer correctness via `tool_context.state` with SHA-256 computation, AR-CRIT-001 compliance (zero `session.state` violations), and reader consumption in both the loop-detection component and the generic state pipeline. The one caveat is that this is static analysis — runtime behavior (e.g., that `self._depth` is correctly propagated from the orchestrator through to `REPLTool` construction at depth > 0) is not exercised here, though the constructor wiring at `repl_tool.py:70,89` confirms the parameter path exists.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's evidence is direct and substantive, not superficial. The critical validation dimension — AR-CRIT-001 compliance — is backed by two complementary pieces of evidence: (1) a cited write at `repl_tool.py:127` showing `tool_context.state[depth_key(...)]` (the correct mutation path), and (2) a negative grep confirming zero `session.state` references in the file. The SHA-256 computation is shown at the specific line, depth-scoping is confirmed both via `DEPTH_SCOPED_KEYS` membership and the actual `depth_key()` call in the writer, and two distinct readers are traced with line-level citations. The Implementer also honestly disclosed the limitation of static-only analysis regarding runtime depth propagation, which strengthens rather than weakens credibility.

REWARD_HACK_DETECTED: no
