        # Validation Report: LC-004 — Select provider-fake dropdown

        **Category**: launch_controls
        **Validated**: 2026-03-23 04:27
        **Verdict**: PASS

        ---

        ## Validation Report: LC-004

### Summary
**PASS** — The provider-fake dropdown is fully implemented: populated from disk via `list_provider_fake_fixtures()`, wired to `on_pf_change` → `controller.set_provider_fake_fixture()`, with correct mutual exclusion against the replay dropdown, and the selected fixture correctly feeds into `launch_replay()` → `prepare_provider_fake_launch()`.

### Evidence

1. **UI rendering** (`live_app.py:482-488`): `ui.select` is rendered with `options=pf_options`, `value=controller.state.selected_provider_fake_fixture or None`, `label="Select provider-fake"`, and `on_change=on_pf_change`. Styled with `flex: 1 1 14rem; min-width: 14rem;`.

2. **Options source** (`live_app.py:369`): `pf_options = controller.state.available_provider_fake_fixtures` — directly reads from controller state.

3. **Handler wiring** (`live_app.py:400-402`): `on_pf_change(e)` calls `controller.set_provider_fake_fixture(str(e.value or ""))` then `live_ui.refresh_all()`.

4. **Controller population** (`live_controller.py:63`): `initialize()` sets `self.state.available_provider_fake_fixtures = list_provider_fake_fixtures()`.

5. **Mutual exclusion** (`live_controller.py:81-85`): `set_provider_fake_fixture()` sets `selected_provider_fake_fixture = fixture_stem` and if truthy, clears `replay_path = ""`. Symmetric logic exists in `set_replay_path()` (`live_controller.py:75-79`) which clears `selected_provider_fake_fixture` when a replay is chosen.

6. **Launch delegation** (`live_controller.py:96-100`): `launch_replay()` checks `selected_provider_fake_fixture` first — if set, calls `prepare_provider_fake_launch(self.state.selected_provider_fake_fixture, enabled_skills=...)`.

7. **Fixture enumeration** (`run_service.py:145-165`): `list_provider_fake_fixtures()` globs `*.json` from `tests_rlm_adk/fixtures/provider_fake/`, returns sorted stems (filename without `.json`). Confirmed fixture directory has files on disk.

8. **Launch execution** (`run_service.py:266-288`): `prepare_provider_fake_launch()` resolves the stem back to a file path via `resolve_fixture_file_path("provider_fake", fixture_stem)`, creates `ScenarioRouter` and `FakeGeminiServer`, returning a `ProviderFakeLaunchHandle`.

9. **State model** (`live_models.py:337-338`): `available_provider_fake_fixtures: list[str]` and `selected_provider_fake_fixture: str = ""` are declared in `LiveDashboardState`.

### Bugs Found
None.

### Validation Notes
All three aspects of the validation strategy were confirmed: (a) the dropdown is populated from `list_provider_fake_fixtures()` during `controller.initialize()`, which enumerates real `.json` fixture stems from the provider-fake directory on disk; (b) selecting a value triggers `set_provider_fake_fixture()` which sets state and clears the replay path for mutual exclusion; (c) `launch_replay()` delegates to `prepare_provider_fake_launch()` with the stored fixture stem. The data flow is end-to-end complete from filesystem enumeration through UI rendering through controller state mutation through launch execution. The only caveat is that this is static validation — runtime behavior (e.g., NiceGUI reactivity, actual `on_change` event shape) was not exercised.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence is thorough and directly validates the manifest's three requirements: (1) a `ui.select` dropdown exists with provider-fake fixtures as options (evidence #1, #2), (2) the handler chain `on_pf_change` → `controller.set_provider_fake_fixture()` is explicitly traced with line numbers (#3), and (3) the downstream data flow from fixture selection through launch execution is documented end-to-end (#4–#8). The report goes beyond surface-level checks by tracing the full pipeline from filesystem enumeration through state mutation through launch delegation, citing specific line numbers and code behavior at each step. The honest caveat about static-only validation (no runtime NiceGUI event exercise) is appropriate transparency, not a gap — the manifest asks for wiring validation, and the wiring is confirmed.

REWARD_HACK_DETECTED: no
