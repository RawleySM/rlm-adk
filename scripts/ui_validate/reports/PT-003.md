        # Validation Report: PT-003 — ui.refreshable invocation_section

        **Category**: polling_timers
        **Validated**: 2026-03-23 07:34
        **Verdict**: PASS

        ---

        ## Validation Report: PT-003

### Summary
**PASS** — `invocation_section` is decorated with `@ui.refreshable`, registered with `live_ui.register()`, and wired into the `_poll()` → `controller.poll()` → `refresh_all()` chain at a 1.0s interval.

### Evidence

1. **Decorator presence** (`live_app.py:267-268`):
   ```python
   @ui.refreshable
   def invocation_section() -> None:
   ```
   The `@ui.refreshable` decorator is syntactically applied directly to `invocation_section`.

2. **Registration call** (`live_app.py:302`):
   ```python
   live_ui.register(invocation_section)
   ```
   This appends `invocation_section` to `LiveDashboardUI._refreshables`.

3. **`LiveDashboardUI.register` implementation** (`live_controller.py:702-703`):
   ```python
   def register(self, refreshable_fn) -> None:
       self._refreshables.append(refreshable_fn)
   ```
   Confirms the function is stored in the `_refreshables` list.

4. **`LiveDashboardUI.refresh_all` implementation** (`live_controller.py:705-707`):
   ```python
   def refresh_all(self) -> None:
       for refreshable in self._refreshables:
           refreshable.refresh()
   ```
   Iterates all registered refreshables and calls `.refresh()` on each — including `invocation_section`.

5. **Poll timer wiring** (`live_app.py:340`):
   ```python
   ui.timer(1.0, _poll)
   ```
   A 1.0-second NiceGUI timer drives the `_poll` function.

6. **`_poll` → `refresh_all` chain** (`live_app.py:325-336`):
   ```python
   async def _poll() -> None:
       changed = await controller.poll()
       ...
       if (changed or controller.state.launch_in_progress or ...):
           live_ui.refresh_all()
   ```
   On each tick, `_poll` calls `controller.poll()`, and if data changed (or launch is active), calls `live_ui.refresh_all()`, which triggers `invocation_section.refresh()`.

7. **Initial render** (`live_app.py:322`):
   ```python
   invocation_section()
   ```
   The refreshable is invoked once during page setup to produce the initial DOM.

### Bugs Found
None.

### Validation Notes
All five links in the refresh chain are verified: (1) `@ui.refreshable` decorator on `invocation_section`, (2) registration via `live_ui.register(invocation_section)`, (3) `_refreshables` list storage, (4) `refresh_all()` iterating and calling `.refresh()`, and (5) `ui.timer(1.0, _poll)` driving the poll-to-refresh cycle. The wiring is complete and correct. The only caveat is that `refresh_all()` is conditional on `changed` or launch-in-progress flags — this is intentional optimization, not a gap, as idle sessions skip unnecessary re-renders.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The implementer traced the complete refresh chain from decorator application through registration, storage, polling, and invocation. The evidence is not superficial — it goes beyond confirming the decorator exists and verifies the full wiring: `@ui.refreshable` → `register()` → `_refreshables` list → `refresh_all()` → `.refresh()` call, driven by `ui.timer(1.0, _poll)`. Line numbers and code snippets are cited at each step, making the claims verifiable. The conditional refresh note (only when `changed` or launch flags are set) is correctly identified as intentional behavior rather than a gap. The initial render call is also confirmed.

REWARD_HACK_DETECTED: no
