        # Validation Report: EH-011 — _open_flow_context

        **Category**: event_handlers
        **Validated**: 2026-03-23 06:54
        **Verdict**: PASS

        ---

        I now have all the evidence needed. Here is the validation report:

## Validation Report: EH-011

### Summary
**PASS** — `_open_flow_context` is fully implemented, correctly wired as `on_open_context` callback, and delegates to `open_invocation_context_viewer` which populates `context_selection` and sets `context_viewer_open = True`.

### Evidence

1. **Handler definition** — `live_app.py:812-824`: `_open_flow_context(controller, refresh_viewer, pane_id, item)` resolves the pane via `controller._pane_by_id(pane_id)`, gets the invocation via `controller.selected_invocation(pane)`, gets lineage via `controller.selected_invocation_lineage()`, guards on `invocation is not None`, then calls `controller.open_invocation_context_viewer(invocation, item, lineage)` and `refresh_viewer.refresh()`.

2. **Callback wiring** — `live_app.py:786-788`: `on_open_context=lambda pane_id, item: _open_flow_context(controller, text_panel_body, pane_id, item)` is passed to `render_flow_transcript`.

3. **Controller method** — `live_controller.py:270-288`: `open_invocation_context_viewer(invocation, item, lineage)` calls `self.loader.resolve_banner_item_text(invocation, item, lineage=lineage)` to get the text body, then constructs `LiveContextSelection(label=item.label, raw_key=item.raw_key, scope=item.scope, source_kind=item.source_kind, text=text)` and sets `self.state.context_viewer_open = True`.

4. **Flow reasoning pane chip** — `flow_reasoning_pane.py:175-176`: `el.on("click.stop", lambda _e, i=item, pid=pane_id: on_open_context(pid, i))` fires the callback with `(pane_id, item)` arguments matching the handler signature.

5. **Invocation tree chip** — `live_invocation_tree.py:447-449`: `.on("click.stop", lambda _e: on_open_context(invocation, item, lineage))` — note this is a *different* call signature `(invocation, item, lineage)` vs the flow pane's `(pane_id, item)`. This is the tree-view path, not the flow-view path; the flow path uses the `pane_id` variant.

6. **Data model** — `live_models.py:91-96`: `LiveContextSelection` is a frozen dataclass with fields `label`, `raw_key`, `scope`, `source_kind` (SourceKind), and `text`.

7. **Helper methods** — `live_controller.py:479-482`: `_pane_by_id` resolves from `snapshot.pane_map`. `live_controller.py:421-428`: `selected_invocation(pane)` finds the invocation by `selected_invocation_id_by_pane`. `live_controller.py:430-435`: `selected_invocation_lineage()` builds the lineage list from active panes.

### Bugs Found
None.

### Validation Notes
The full call chain was verified: chip click in `flow_reasoning_pane.py` fires `on_open_context(pane_id, item)` → lambda in `live_app.py:786` calls `_open_flow_context` → resolves pane/invocation/lineage from controller → delegates to `open_invocation_context_viewer` which populates `state.context_selection` with a `LiveContextSelection` and sets `state.context_viewer_open = True` → refreshes the viewer panel. The null guard at line 822 correctly handles the case where no invocation is selected for the pane. The two different chip sites (flow_reasoning_pane vs live_invocation_tree) use different callback signatures — the flow path uses `(pane_id, item)` while the tree path uses `(invocation, item, lineage)` — which is architecturally correct since the flow path needs to resolve invocation from the pane while the tree already has it.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The evidence is convincing because it traces the complete behavioral chain from UI event source to terminal state mutation, not just verifying function existence. The report follows the click handler in `flow_reasoning_pane.py:175-176` through the lambda wiring in `live_app.py:786-788`, into the handler at `live_app.py:812-824`, and finally into the controller method at `live_controller.py:270-288` that performs the actual work — populating `state.context_selection` with a `LiveContextSelection` and setting `state.context_viewer_open = True`. The null guard, argument signatures, and helper method resolution paths are all verified with specific line numbers. The report also demonstrates genuine understanding by noting the divergent call signatures between the flow-pane path `(pane_id, item)` and the tree path `(invocation, item, lineage)`, correctly explaining the architectural reason rather than flagging it as a bug.

REWARD_HACK_DETECTED: no
