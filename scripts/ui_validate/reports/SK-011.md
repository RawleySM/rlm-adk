        # Validation Report: SK-011 — REPL_EXPANDED_CODE_HASH

        **Category**: state_keys
        **Validated**: 2026-03-23 00:07
        **Verdict**: PASS

        ---

        ## Validation Report: SK-011

### Summary
**PASS** — All four properties of the `REPL_EXPANDED_CODE_HASH` state key are correctly implemented: constant definition, depth-scoping, SHA-256 writer in `repl_tool.py`, and sqlite_tracing capture via prefix match. The manifest's claimed reader (`live_loader.py`) is inaccurate but this is a documentation issue, not a code bug.

### Evidence

1. **Constant definition** (`state.py:82`): `REPL_EXPANDED_CODE_HASH = "repl_expanded_code_hash"` — correct string value.

2. **DEPTH_SCOPED_KEYS membership** (`state.py:156`): `REPL_EXPANDED_CODE_HASH` is listed in the `DEPTH_SCOPED_KEYS` set, confirming `@dN` suffixing at depth > 0.

3. **depth_key() function** (`state.py:166-174`): Returns bare key at depth 0, suffixed `@dN` at depth N > 0. Used correctly at the writer site.

4. **Writer site** (`repl_tool.py:188-190`):
   - `exec_code_hash = hashlib.sha256(exec_code.encode()).hexdigest()` — produces 64-char hex SHA-256.
   - `tool_context.state[depth_key(REPL_EXPANDED_CODE_HASH, self._depth)] = exec_code_hash` — writes via `tool_context.state` (correct per AR-CRIT-001), with proper depth scoping.
   - Guarded by `if expansion.did_expand:` — only written when skill expansion actually occurs.

5. **sqlite_tracing capture** (`state.py:222`): `CURATED_STATE_PREFIXES` contains `"repl_expanded_code"`, which is a prefix of `"repl_expanded_code_hash"`. Therefore `should_capture_state_key("repl_expanded_code_hash")` returns `True` via the `startswith` check at line 234.

6. **Dashboard does NOT read this key**: grep of `rlm_adk/dashboard/` for `repl_expanded_code_hash` or `REPL_EXPANDED_CODE_HASH` returned **zero matches**. The manifest's claimed reader `live_loader.py` is inaccurate — the dashboard reads the sibling `REPL_EXPANDED_CODE` (full text), not the hash.

### Bugs Found

- **Bug**: Manifest claims `live_loader.py` as the reader for `REPL_EXPANDED_CODE_HASH`, but no reference to this key exists anywhere in the dashboard directory.
- **Location**: Manifest metadata (not a code bug)
- **Severity**: cosmetic — the hash is still captured via `sqlite_tracing` through the prefix-based `should_capture_state_key()`, so no data is lost. The manifest is simply misleading.
- **Fix**: Update the manifest's reader field from `live_loader.py` to `sqlite_tracing.py (via should_capture_state_key prefix match)`.

### Validation Notes

All code-level properties were validated via direct file reads: the constant is correctly defined with the expected string value, it is a member of `DEPTH_SCOPED_KEYS`, the writer in `repl_tool.py` produces a proper SHA-256 hexdigest and writes it through `tool_context.state` with depth scoping (compliant with AR-CRIT-001), and the prefix `"repl_expanded_code"` in `CURATED_STATE_PREFIXES` ensures sqlite_tracing captures it. The only discrepancy is the manifest's reader attribution — `live_loader.py` does not reference this hash key. This is a static analysis; runtime behavior (e.g., that `expansion.did_expand` is `True` in specific fixtures) was not exercised but the code paths are unambiguous.

        ---

        ## Reviewer Assessment

        VERDICT: PASS

REASONING:
The Implementer's report demonstrates genuine code-level validation, not superficial checks. The writer path was verified down to the specific line numbers showing `hashlib.sha256(...).hexdigest()` written via `tool_context.state` (not `ctx.session.state`), which is the critical AR-CRIT-001 compliance check. Depth-scoping was validated through both `DEPTH_SCOPED_KEYS` membership and the actual `depth_key()` call at the write site. The Implementer also caught a real manifest inaccuracy — `live_loader.py` does not read this key — and correctly classified it as cosmetic rather than hiding it. The prefix-match explanation for `sqlite_tracing` capture is mechanistically sound (prefix `"repl_expanded_code"` matching `"repl_expanded_code_hash"` via `startswith`). The one limitation — no runtime exercise of the `expansion.did_expand` guard — is honestly disclosed and reasonable for static validation.

REWARD_HACK_DETECTED: no
